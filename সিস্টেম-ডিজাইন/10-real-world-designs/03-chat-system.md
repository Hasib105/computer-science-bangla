# WhatsApp/Chat System ডিজাইন

## 🎯 প্রবলেম স্টেটমেন্ট

```
WhatsApp-এর মতো একটি রিয়েল-টাইম মেসেজিং সিস্টেম ডিজাইন করুন।
```

## 📊 Requirements

### Functional Requirements
```
১. 1-on-1 chat
২. Group chat (up to 256 members)
৩. Message delivery status (sent, delivered, read)
৪. Online/Offline status
৫. Media sharing (images, videos)
৬. Push notifications
৭. End-to-end encryption
```

### Non-Functional Requirements
```
১. Real-time messaging (< 100ms latency)
২. High availability
৩. Message ordering guarantee
৪. Offline message support
```

### Scale Estimation
```
Assumptions:
- 2B users
- 50M daily active users
- Each user sends 40 messages/day
- Message size: 100 bytes average

Calculations:
- 50M × 40 = 2B messages/day
- 2B / 86400 = 23,000 messages/second
- Peak: 23,000 × 3 = 70,000 messages/second

Storage:
- 2B messages × 100 bytes = 200 GB/day
- Per year: 73 TB
```

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Chat System Architecture                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐                              ┌──────────┐        │
│  │  User A  │                              │  User B  │        │
│  │  Mobile  │                              │  Mobile  │        │
│  └────┬─────┘                              └────┬─────┘        │
│       │                                         │               │
│       │ WebSocket                    WebSocket │               │
│       │                                         │               │
│  ┌────┴─────────────────────────────────────────┴────┐         │
│  │              WebSocket Gateway Cluster             │         │
│  │     (Maintains persistent connections)             │         │
│  └────────────────────────┬──────────────────────────┘         │
│                           │                                     │
│  ┌────────────────────────┼────────────────────────┐           │
│  │                        │                        │           │
│  │    ┌──────────────┐   │   ┌──────────────┐    │           │
│  │    │   Message    │   │   │   Presence   │    │           │
│  │    │   Service    │   │   │   Service    │    │           │
│  │    └──────┬───────┘   │   └──────────────┘    │           │
│  │           │           │                        │           │
│  │    ┌──────┴───────────┴────────────────┐     │           │
│  │    │            Message Queue           │     │           │
│  │    │              (Kafka)               │     │           │
│  │    └──────────────────┬─────────────────┘     │           │
│  │                       │                        │           │
│  │    ┌──────────────────┴────────────────┐     │           │
│  │    │           Data Stores              │     │           │
│  │    │  ┌─────────┐    ┌─────────┐       │     │           │
│  │    │  │Messages │    │  Users  │       │     │           │
│  │    │  │   DB    │    │   DB    │       │     │           │
│  │    │  └─────────┘    └─────────┘       │     │           │
│  │    └────────────────────────────────────┘     │           │
│  │                                                │           │
│  └────────────────────────────────────────────────┘           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🔌 WebSocket Connection

```
Why WebSocket?

HTTP Polling:
Client ─── Request ──→ Server
Client ←── Response ─── Server
(Every few seconds - inefficient)

WebSocket:
Client ←────────────→ Server
(Persistent bidirectional connection)

┌──────────────────────────────────────────────────────────┐
│              WebSocket Connection Flow                   │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Client                    Server                        │
│    │                         │                           │
│    │── HTTP Upgrade ────────→│                           │
│    │                         │                           │
│    │←── 101 Switching ───────│                           │
│    │     Protocols           │                           │
│    │                         │                           │
│    │←══════ WebSocket ══════→│                           │
│    │    (Persistent)         │                           │
│    │                         │                           │
│    │── Message ─────────────→│                           │
│    │←── Message ─────────────│                           │
│    │                         │                           │
└──────────────────────────────────────────────────────────┘
```

## 📨 Message Flow

### 1-on-1 Chat
```
User A → Server → User B

┌──────────────────────────────────────────────────────────┐
│                  Message Delivery Flow                   │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  User A                Server                 User B     │
│    │                     │                      │        │
│    │── Send Message ────→│                      │        │
│    │   (to User B)       │                      │        │
│    │                     │── Store in DB        │        │
│    │←── Sent ✓ ──────────│                      │        │
│    │                     │                      │        │
│    │                     │── Forward ──────────→│        │
│    │                     │                      │        │
│    │                     │←── Delivered ACK ────│        │
│    │←── Delivered ✓✓ ────│                      │        │
│    │                     │                      │        │
│    │                     │←── Read ACK ─────────│        │
│    │←── Read ✓✓ (blue) ──│                      │        │
│    │                     │                      │        │
└──────────────────────────────────────────────────────────┘
```

### Offline User
```
User B অফলাইন থাকলে:

User A → Server → Store Message
                      │
                      ├── Push Notification → User B's phone
                      │
                      └── Message stored until B comes online
                      
User B অনলাইন হলে:
Server → Fetch pending messages → Deliver to User B
```

## 📦 Data Models

```sql
-- Users
CREATE TABLE users (
    user_id BIGINT PRIMARY KEY,
    phone VARCHAR(20) UNIQUE,
    name VARCHAR(100),
    avatar_url TEXT,
    last_seen TIMESTAMP,
    created_at TIMESTAMP
);

-- Conversations
CREATE TABLE conversations (
    conversation_id BIGINT PRIMARY KEY,
    type ENUM('private', 'group'),
    created_at TIMESTAMP
);

-- Conversation Participants
CREATE TABLE participants (
    conversation_id BIGINT,
    user_id BIGINT,
    joined_at TIMESTAMP,
    PRIMARY KEY (conversation_id, user_id)
);

-- Messages
CREATE TABLE messages (
    message_id BIGINT PRIMARY KEY,
    conversation_id BIGINT,
    sender_id BIGINT,
    content TEXT,
    media_url TEXT,
    sent_at TIMESTAMP,
    
    INDEX idx_conversation (conversation_id, sent_at DESC)
);

-- Message Status
CREATE TABLE message_status (
    message_id BIGINT,
    user_id BIGINT,
    status ENUM('sent', 'delivered', 'read'),
    updated_at TIMESTAMP,
    PRIMARY KEY (message_id, user_id)
);
```

## 🔒 End-to-End Encryption

```
Signal Protocol (WhatsApp uses):

Key Generation:
User A: Generate public/private key pair
User B: Generate public/private key pair

Key Exchange:
User A ── Public Key A ──→ Server
User B ── Public Key B ──→ Server

Encryption:
Message + B's Public Key = Encrypted Message

Only User B can decrypt with their private key.

┌────────────────────────────────────────────────────────┐
│               E2E Encryption Flow                      │
├────────────────────────────────────────────────────────┤
│                                                        │
│  User A                              User B            │
│    │                                   │               │
│    │── Encrypt(msg, B_pub_key) ───────→│               │
│    │                                   │               │
│    │   Server sees: 0xA3F2B1C4...     │               │
│    │   (Cannot read message)           │               │
│    │                                   │               │
│    │           Decrypt(msg, B_priv_key)│               │
│    │                                   │               │
│    │            "Hello!" ──────────────│               │
│                                                        │
└────────────────────────────────────────────────────────┘
```

## ⚡ Key Components

### Gateway Server
```
- WebSocket connection management
- 1M+ concurrent connections per server
- Session mapping: user_id → server_id

Redis for session:
user:123:server = "gateway-server-7"
```

### Presence Service
```
Online/Offline status:

Last Active:
- Update every message/action
- TTL 30 seconds

Redis:
user:123:online = 1 (TTL 30s)
user:123:last_seen = "2024-01-26T10:30:00Z"
```

### Group Chat
```
Group with 256 members:

Message → Kafka → Fanout to 256 members

┌──────────────────────────────────────────┐
│            Group Message Flow            │
├──────────────────────────────────────────┤
│                                          │
│  Sender → Server → Kafka                 │
│                       │                  │
│                       ├→ Member 1        │
│                       ├→ Member 2        │
│                       ├→ Member 3        │
│                       └→ ... (256)       │
│                                          │
└──────────────────────────────────────────┘
```

## ✅ Summary

```
Key Design Points:
1. WebSocket for real-time bidirectional communication
2. Message queue for async processing
3. Sharding by conversation_id
4. Redis for session & presence
5. E2E encryption for security
6. Push notifications for offline users
```

## 📚 পরবর্তী টপিক

[YouTube ডিজাইন →](./04-youtube-design.md)
