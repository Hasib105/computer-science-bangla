# নেটওয়ার্ক প্রটোকল (Network Protocols)

## 🎯 প্রটোকল কি?

**প্রটোকল** হলো নিয়মের সেট যা নির্ধারণ করে কিভাবে দুটি সিস্টেম একে অপরের সাথে যোগাযোগ করবে।

```
মানুষের ভাষা = বাংলা, ইংরেজি
কম্পিউটারের ভাষা = TCP, UDP, HTTP
```

## 📚 OSI মডেল (৭ লেয়ার)

```
┌─────────────────────────────────────────────────────────┐
│ লেয়ার ৭: Application  │ HTTP, FTP, SMTP, DNS         │
├─────────────────────────────────────────────────────────┤
│ লেয়ার ৬: Presentation │ এনক্রিপশন, কম্প্রেশন, ফরম্যাট │
├─────────────────────────────────────────────────────────┤
│ লেয়ার ৫: Session      │ সেশন ম্যানেজমেন্ট            │
├─────────────────────────────────────────────────────────┤
│ লেয়ার ৪: Transport    │ TCP, UDP                     │
├─────────────────────────────────────────────────────────┤
│ লেয়ার ৩: Network      │ IP, ICMP, রাউটিং             │
├─────────────────────────────────────────────────────────┤
│ লেয়ার ২: Data Link    │ MAC অ্যাড্রেস, ইথারনেট        │
├─────────────────────────────────────────────────────────┤
│ লেয়ার ১: Physical     │ ক্যাবল, ওয়্যারলেস, সিগন্যাল  │
└─────────────────────────────────────────────────────────┘
```

## 🔌 TCP/IP মডেল (৪ লেয়ার)

```
┌────────────────────────────────────────┐
│  Application Layer                      │
│  (HTTP, FTP, SMTP, DNS, SSH)           │
├────────────────────────────────────────┤
│  Transport Layer                        │
│  (TCP, UDP)                            │
├────────────────────────────────────────┤
│  Internet Layer                         │
│  (IP, ICMP, ARP)                       │
├────────────────────────────────────────┤
│  Network Access Layer                   │
│  (Ethernet, WiFi)                      │
└────────────────────────────────────────┘
```

## 📡 TCP (Transmission Control Protocol)

### TCP কি?
**TCP** হলো একটি রিলায়েবল, কানেকশন-ওরিয়েন্টেড প্রটোকল।

### TCP 3-Way Handshake
```
ক্লায়েন্ট                              সার্ভার
    │                                    │
    │ ────── SYN ──────────────────→    │  ১. সংযোগ অনুরোধ
    │                                    │
    │ ←───── SYN-ACK ─────────────     │  ২. অনুরোধ গ্রহণ
    │                                    │
    │ ────── ACK ──────────────────→    │  ৩. সংযোগ স্থাপিত
    │                                    │
    │ ←──── ডাটা আদান-প্রদান ────→      │
    │                                    │

SYN = Synchronize
ACK = Acknowledge
```

### TCP বৈশিষ্ট্য
```
✓ রিলায়েবল ডেলিভারি - হারিয়ে যাওয়া প্যাকেট পুনরায় পাঠানো
✓ অর্ডার্ড ডেলিভারি - সঠিক ক্রমে ডাটা পৌঁছায়
✓ এরর চেকিং - চেকসাম ভেরিফিকেশন
✓ ফ্লো কন্ট্রোল - ওভারলোড প্রতিরোধ
✓ কনজেশন কন্ট্রোল - নেটওয়ার্ক জ্যাম প্রতিরোধ
```

### TCP ব্যবহার
```
- HTTP/HTTPS (ওয়েব ব্রাউজিং)
- FTP (ফাইল ট্রান্সফার)
- SMTP (ইমেইল)
- SSH (রিমোট অ্যাক্সেস)
- ডাটাবেস কানেকশন
```

## 📨 UDP (User Datagram Protocol)

### UDP কি?
**UDP** হলো একটি ফাস্ট, কানেকশনলেস প্রটোকল।

### UDP বৈশিষ্ট্য
```
✓ দ্রুত - কোনো হ্যান্ডশেক নেই
✓ লাইটওয়েট - কম ওভারহেড
✗ আনরিলায়েবল - প্যাকেট হারিয়ে যেতে পারে
✗ আনঅর্ডার্ড - ভুল ক্রমে আসতে পারে
```

### UDP ব্যবহার
```
- ভিডিও স্ট্রিমিং
- অনলাইন গেমিং
- VoIP (ভয়েস কল)
- DNS রিকোয়েস্ট
- লাইভ ব্রডকাস্ট
```

## 🆚 TCP vs UDP

```
┌────────────────┬─────────────────┬─────────────────┐
│     বিষয়       │      TCP        │      UDP        │
├────────────────┼─────────────────┼─────────────────┤
│ কানেকশন       │ কানেকশন-ভিত্তিক │ কানেকশনলেস     │
│ রিলায়েবিলিটি │ গ্যারান্টিড     │ কোনো গ্যারান্টি নেই│
│ স্পিড         │ স্লো           │ ফাস্ট           │
│ অর্ডার        │ সঠিক ক্রম      │ যেকোনো ক্রম     │
│ হেডার সাইজ   │ ২০ বাইট        │ ৮ বাইট          │
│ ব্যবহার       │ ওয়েব, ইমেইল   │ গেমিং, স্ট্রিমিং │
└────────────────┴─────────────────┴─────────────────┘
```

## 🌐 HTTP (HyperText Transfer Protocol)

### HTTP ভার্সন
```
HTTP/1.0 (১৯৯৬)
├── প্রতি রিকোয়েস্টে নতুন কানেকশন
└── স্লো

HTTP/1.1 (১৯৯৭)
├── Keep-Alive কানেকশন
├── পাইপলাইনিং
└── হোস্ট হেডার

HTTP/2 (২০১৫)
├── মাল্টিপ্লেক্সিং
├── হেডার কম্প্রেশন
├── সার্ভার পুশ
└── বাইনারি প্রটোকল

HTTP/3 (২০২২)
├── QUIC প্রটোকল (UDP-ভিত্তিক)
├── দ্রুততর কানেকশন
└── বেটার পারফরম্যান্স
```

### HTTP রিকোয়েস্ট স্ট্রাকচার
```
GET /api/users HTTP/1.1
Host: example.com
Content-Type: application/json
Authorization: Bearer token123

{
  "name": "আহমেদ"
}

ব্যাখ্যা:
- Line 1: মেথড, পাথ, ভার্সন
- Line 2-4: হেডার
- Line 6-8: বডি
```

### HTTP রেসপন্স স্ট্রাকচার
```
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 256

{
  "id": 1,
  "name": "আহমেদ"
}
```

## 🔒 HTTPS/TLS

### TLS Handshake
```
ক্লায়েন্ট                                সার্ভার
    │                                      │
    │ ── Client Hello ──────────────→     │
    │    (সাপোর্টেড সাইফার)                │
    │                                      │
    │ ←─ Server Hello ────────────────    │
    │    (সার্টিফিকেট + সাইফার)            │
    │                                      │
    │ ── Key Exchange ──────────────→     │
    │    (প্রিমাস্টার সিক্রেট)             │
    │                                      │
    │ ←─ Finished ────────────────────    │
    │                                      │
    │ ══ এনক্রিপ্টেড কমিউনিকেশন ══        │
```

## 🔍 DNS (Domain Name System)

### DNS কি?
DNS ডোমেইন নাম (example.com) কে IP অ্যাড্রেসে (93.184.216.34) রূপান্তর করে।

### DNS রেজোল্যুশন প্রক্রিয়া
```
                    ┌─────────────────┐
                    │   রুট DNS       │
                    │   সার্ভার        │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
┌───────┴───────┐   ┌────────┴───────┐   ┌───────┴───────┐
│   .com TLD    │   │   .org TLD     │   │   .bd TLD     │
└───────┬───────┘   └────────────────┘   └───────────────┘
        │
┌───────┴───────────────┐
│   example.com         │
│   Authoritative DNS   │
└───────────────────────┘

প্রক্রিয়া:
১. ব্রাউজার → লোকাল ক্যাশ চেক
২. লোকাল ক্যাশে নেই → ISP DNS সার্ভার
৩. ISP ক্যাশে নেই → রুট DNS
৪. রুট → TLD সার্ভার (.com)
৫. TLD → Authoritative সার্ভার
৬. IP অ্যাড্রেস রিটার্ন
```

### DNS রেকর্ড টাইপ
```
A Record      → ডোমেইন → IPv4 অ্যাড্রেস
AAAA Record   → ডোমেইন → IPv6 অ্যাড্রেস
CNAME         → ডোমেইন → অন্য ডোমেইন (এলিয়াস)
MX            → মেইল সার্ভার
NS            → নেম সার্ভার
TXT           → টেক্সট ইনফরমেশন
TTL           → ক্যাশ টাইম
```

## 🔗 WebSocket

### WebSocket কি?
ক্লায়েন্ট ও সার্ভারের মধ্যে ফুল-ডুপ্লেক্স, পার্সিস্টেন্ট কানেকশন।

```
HTTP:
Request → Response (প্রতিবার নতুন)

WebSocket:
┌─────────────────────────────────────┐
│     ক্লায়েন্ট ←──────→ সার্ভার      │
│     (সবসময় কানেক্টেড)              │
└─────────────────────────────────────┘
```

### WebSocket Handshake
```
ক্লায়েন্ট:
GET /chat HTTP/1.1
Upgrade: websocket
Connection: Upgrade

সার্ভার:
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
```

### WebSocket ব্যবহার
```
- রিয়েল-টাইম চ্যাট
- লাইভ নোটিফিকেশন
- অনলাইন গেম
- স্টক মার্কেট ডাটা
- কোলাবোরেটিভ এডিটিং
```

## 📧 অন্যান্য গুরুত্বপূর্ণ প্রটোকল

### SMTP (Simple Mail Transfer Protocol)
```
ব্যবহার: ইমেইল পাঠানো
পোর্ট: ২৫, ৫৮৭
প্রক্রিয়া: MUA → MSA → MTA → MTA → MDA → Mailbox
```

### FTP (File Transfer Protocol)
```
ব্যবহার: ফাইল ট্রান্সফার
পোর্ট: ২১ (কন্ট্রোল), ২০ (ডাটা)
মোড: অ্যাক্টিভ, প্যাসিভ
```

### SSH (Secure Shell)
```
ব্যবহার: সিকিউর রিমোট অ্যাক্সেস
পোর্ট: ২২
বৈশিষ্ট্য: এনক্রিপশন, অথেন্টিকেশন
```

## 🎯 সাধারণ পোর্ট নম্বর

| পোর্ট | প্রটোকল | ব্যবহার |
|-------|---------|---------|
| 20/21 | FTP | ফাইল ট্রান্সফার |
| 22 | SSH | সিকিউর শেল |
| 25 | SMTP | মেইল পাঠানো |
| 53 | DNS | ডোমেইন রেজোল্যুশন |
| 80 | HTTP | ওয়েব ব্রাউজিং |
| 443 | HTTPS | সিকিউর ওয়েব |
| 3306 | MySQL | ডাটাবেস |
| 5432 | PostgreSQL | ডাটাবেস |
| 6379 | Redis | ক্যাশ |
| 27017 | MongoDB | ডাটাবেস |

## 📚 পরবর্তী টপিক

[লেটেন্সি ও থ্রুপুট →](./04-latency-throughput.md)
