# ক্লায়েন্ট-সার্ভার আর্কিটেকচার (Client-Server Architecture)

## 🎯 সংজ্ঞা

**ক্লায়েন্ট-সার্ভার মডেল** হলো একটি ডিস্ট্রিবিউটেড অ্যাপ্লিকেশন স্ট্রাকচার যেখানে কাজ ভাগ করা হয়:
- **ক্লায়েন্ট:** যে রিকোয়েস্ট পাঠায়
- **সার্ভার:** যে রিকোয়েস্ট প্রসেস করে এবং রেসপন্স দেয়

## 📊 বেসিক আর্কিটেকচার

```
┌────────────────┐                    ┌────────────────┐
│    ক্লায়েন্ট   │   HTTP Request    │     সার্ভার    │
│   (Browser,    │ ─────────────────→ │   (Apache,     │
│    Mobile App) │                    │    Nginx,      │
│                │ ←───────────────── │    Node.js)    │
│                │   HTTP Response    │                │
└────────────────┘                    └────────────────┘
```

## 🔄 রিকোয়েস্ট-রেসপন্স সাইকেল

### ধাপে ধাপে প্রক্রিয়া

```
১. ইউজার ব্রাউজারে URL টাইপ করে
          ↓
২. DNS রেজোল্যুশন (ডোমেইন → IP)
          ↓
৩. TCP কানেকশন স্থাপন
          ↓
৪. HTTP রিকোয়েস্ট পাঠানো
          ↓
৫. সার্ভার রিকোয়েস্ট প্রসেস করে
          ↓
৬. HTTP রেসপন্স পাঠানো
          ↓
৭. ব্রাউজার কন্টেন্ট রেন্ডার করে
```

## 🏗️ ক্লায়েন্টের প্রকারভেদ

### ১. থিন ক্লায়েন্ট (Thin Client)
```
বৈশিষ্ট্য:
- সার্ভারের উপর বেশি নির্ভরশীল
- কম প্রসেসিং পাওয়ার প্রয়োজন
- সব লজিক সার্ভারে

উদাহরণ:
- ওয়েব ব্রাউজার
- ক্লাউড-ভিত্তিক অ্যাপ
```

### ২. থিক ক্লায়েন্ট (Thick/Fat Client)
```
বৈশিষ্ট্য:
- লোকাল প্রসেসিং বেশি
- অফলাইনে কাজ করতে পারে
- সার্ভার থেকে ডাটা সিঙ্ক করে

উদাহরণ:
- ডেস্কটপ অ্যাপ্লিকেশন
- মোবাইল অ্যাপ
- গেম ক্লায়েন্ট
```

## 🖥️ সার্ভারের প্রকারভেদ

### ১. ওয়েব সার্ভার
```
কাজ: স্ট্যাটিক কন্টেন্ট সার্ভ করা
উদাহরণ: Nginx, Apache
বৈশিষ্ট্য:
- HTML, CSS, JS ফাইল সার্ভ
- ইমেজ, ভিডিও সার্ভ
- ক্যাশিং
```

### ২. অ্যাপ্লিকেশন সার্ভার
```
কাজ: বিজনেস লজিক এক্সিকিউট করা
উদাহরণ: Node.js, Django, Spring
বৈশিষ্ট্য:
- ডায়নামিক কন্টেন্ট জেনারেট
- ডাটাবেস অপারেশন
- API এন্ডপয়েন্ট
```

### ৩. ডাটাবেস সার্ভার
```
কাজ: ডাটা স্টোর ও রিট্রিভ করা
উদাহরণ: MySQL, PostgreSQL, MongoDB
বৈশিষ্ট্য:
- CRUD অপারেশন
- কুয়েরি প্রসেসিং
- ডাটা ইন্টিগ্রিটি
```

## 📐 আর্কিটেকচার প্যাটার্ন

### ১. সিঙ্গেল-টায়ার (১-Tier)
```
┌─────────────────────────────────┐
│     সব কিছু একই মেশিনে          │
│  ┌─────────────────────────┐   │
│  │ UI + Logic + Database   │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘

ব্যবহার: ডেস্কটপ অ্যাপ (MS Word)
সুবিধা: সহজ সেটআপ
অসুবিধা: স্কেলেবল নয়
```

### ২. টু-টায়ার (২-Tier)
```
┌────────────┐     ┌────────────────────┐
│  ক্লায়েন্ট │ ←→ │  সার্ভার + ডাটাবেস  │
│   (UI)     │     │  (Logic + DB)      │
└────────────┘     └────────────────────┘

ব্যবহার: সরাসরি DB কানেকশন সহ অ্যাপ
সুবিধা: সিম্পল আর্কিটেকচার
অসুবিধা: সিকিউরিটি রিস্ক
```

### ৩. থ্রি-টায়ার (৩-Tier)
```
┌────────────┐   ┌────────────┐   ┌────────────┐
│  ক্লায়েন্ট │ → │  অ্যাপ      │ → │  ডাটাবেস  │
│   (UI)     │   │  সার্ভার    │   │           │
│            │ ← │  (Logic)   │ ← │           │
└────────────┘   └────────────┘   └────────────┘
 প্রেজেন্টেশন     বিজনেস লেয়ার     ডাটা লেয়ার

ব্যবহার: বেশিরভাগ ওয়েব অ্যাপ
সুবিধা: স্কেলেবল, মেইনটেইনেবল
অসুবিধা: জটিলতা বেশি
```

### ৪. N-টায়ার (N-Tier)
```
┌────────────┐   ┌────────────┐   ┌────────────┐   ┌────────────┐
│  ক্লায়েন্ট │ → │    ওয়েব    │ → │    অ্যাপ    │ → │  ডাটাবেস  │
│            │   │   সার্ভার   │   │   সার্ভার   │   │  সার্ভার   │
└────────────┘   └────────────┘   └────────────┘   └────────────┘
                        ↓
                 ┌────────────┐
                 │   ক্যাশ    │
                 │   সার্ভার   │
                 └────────────┘

ব্যবহার: এন্টারপ্রাইজ অ্যাপ্লিকেশন
সুবিধা: হাই স্কেলেবিলিটি
অসুবিধা: জটিলতা ও খরচ বেশি
```

## 🌐 HTTP প্রটোকল

### HTTP মেথড

| মেথড | কাজ | উদাহরণ |
|------|-----|--------|
| GET | ডাটা পড়া | প্রোফাইল দেখা |
| POST | নতুন ডাটা তৈরি | নতুন পোস্ট |
| PUT | ডাটা আপডেট (সম্পূর্ণ) | প্রোফাইল আপডেট |
| PATCH | ডাটা আপডেট (আংশিক) | নাম পরিবর্তন |
| DELETE | ডাটা মুছে ফেলা | পোস্ট ডিলিট |

### HTTP স্ট্যাটাস কোড

```
2xx - সফল (Success)
├── 200 OK - সফল রেসপন্স
├── 201 Created - নতুন রিসোর্স তৈরি
└── 204 No Content - কন্টেন্ট নেই

3xx - রিডাইরেকশন
├── 301 Moved Permanently
├── 302 Found (Temporary Redirect)
└── 304 Not Modified

4xx - ক্লায়েন্ট এরর
├── 400 Bad Request
├── 401 Unauthorized
├── 403 Forbidden
├── 404 Not Found
└── 429 Too Many Requests

5xx - সার্ভার এরর
├── 500 Internal Server Error
├── 502 Bad Gateway
├── 503 Service Unavailable
└── 504 Gateway Timeout
```

## 🔒 HTTPS - সিকিউর কমিউনিকেশন

```
HTTP vs HTTPS

HTTP:
ক্লায়েন্ট ─── প্লেইন টেক্সট ───→ সার্ভার
           (হ্যাকার পড়তে পারে)

HTTPS:
ক্লায়েন্ট ─── এনক্রিপ্টেড ───→ সার্ভার
           (হ্যাকার পড়তে পারে না)

HTTPS এর কাজ:
1. এনক্রিপশন - ডাটা গোপন রাখা
2. অথেন্টিকেশন - সার্ভার যাচাই
3. ইন্টিগ্রিটি - ডাটা পরিবর্তন রোধ
```

## 🔄 কানেকশন টাইপ

### ১. HTTP/1.1 - Keep-Alive
```
আগে (HTTP/1.0):
Request → Response → Connection Close
Request → Response → Connection Close

পরে (HTTP/1.1 Keep-Alive):
Request 1 → Response 1
Request 2 → Response 2  (একই কানেকশন)
Request 3 → Response 3
```

### ২. HTTP/2 - Multiplexing
```
┌──────────────────────────────────────┐
│          একটি TCP কানেকশন            │
│  ┌────────┐  ┌────────┐  ┌────────┐ │
│  │Request1│  │Request2│  │Request3│ │
│  │Response│  │Response│  │Response│ │
│  └────────┘  └────────┘  └────────┘ │
└──────────────────────────────────────┘

সুবিধা:
- দ্রুত লোডিং
- কম লেটেন্সি
- হেডার কম্প্রেশন
```

### ৩. WebSocket - দ্বিমুখী যোগাযোগ
```
HTTP:
Client ──Request──→ Server
Client ←─Response── Server
(প্রতিবার নতুন রিকোয়েস্ট)

WebSocket:
Client ←─────────→ Server
(সবসময় কানেক্টেড, দুদিক থেকে মেসেজ)

ব্যবহার: চ্যাট অ্যাপ, রিয়েল-টাইম গেম, স্টক প্রাইস
```

## 💡 বাস্তব উদাহরণ

### YouTube-এর আর্কিটেকচার
```
┌─────────────┐
│    ইউজার    │
└──────┬──────┘
       │
       ↓
┌──────────────┐     ┌─────────────┐
│     CDN      │ ←── │  ভিডিও স্টোরেজ│
└──────────────┘     └─────────────┘
       │
       ↓
┌──────────────┐     ┌─────────────┐
│  ওয়েব সার্ভার │ ←→ │  ডাটাবেস    │
└──────────────┘     └─────────────┘
       │
       ↓
┌──────────────┐
│ ট্রান্সকোডিং  │
│   সার্ভার     │
└──────────────┘
```

## ✅ সারসংক্ষেপ

| বিষয় | ক্লায়েন্ট | সার্ভার |
|-------|----------|---------|
| কাজ | রিকোয়েস্ট পাঠায় | রিকোয়েস্ট প্রসেস করে |
| অবস্থান | ইউজারের কাছে | ডাটা সেন্টারে |
| উদাহরণ | ব্রাউজার, মোবাইল অ্যাপ | Nginx, Node.js |
| দায়িত্ব | UI প্রদর্শন | ডাটা প্রসেসিং |

## 📚 পরবর্তী টপিক

[নেটওয়ার্ক প্রটোকল →](./03-network-protocols.md)
