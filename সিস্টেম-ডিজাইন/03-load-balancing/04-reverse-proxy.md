# রিভার্স প্রক্সি (Reverse Proxy)

## 🎯 রিভার্স প্রক্সি কি?

**রিভার্স প্রক্সি** হলো সার্ভার ও ক্লায়েন্টের মধ্যে একটি মধ্যস্থতাকারী সার্ভার যা সার্ভারের পক্ষে কাজ করে।

```
Forward Proxy (সাধারণ প্রক্সি):
Client → Proxy → Internet → Server
(ক্লায়েন্টের পক্ষে কাজ করে)

Reverse Proxy:
Client → Internet → Reverse Proxy → Server
(সার্ভারের পক্ষে কাজ করে)
```

## 📊 ফরওয়ার্ড vs রিভার্স প্রক্সি

```
┌─────────────────────────────────────────────────────────────────┐
│                   Forward Proxy                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌────────┐   ┌────────┐        ┌────────┐                    │
│   │Client 1│──→│        │        │        │                    │
│   └────────┘   │Forward │───────→│Internet│───→ Various Servers│
│   ┌────────┐   │ Proxy  │        │        │                    │
│   │Client 2│──→│        │        │        │                    │
│   └────────┘   └────────┘        └────────┘                    │
│                                                                  │
│   ক্লায়েন্ট → প্রক্সি → ইন্টারনেট                               │
│   (সার্ভার জানে না ক্লায়েন্ট কে)                               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   Reverse Proxy                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│                              ┌─────────┐   ┌────────┐          │
│                         ┌───→│ Server1 │   │        │          │
│   ┌────────┐   ┌────────┐   └─────────┘   │        │          │
│   │Internet│──→│Reverse │────────────────→│Origin  │          │
│   │        │   │ Proxy  │   ┌─────────┐   │Servers │          │
│   └────────┘   └────────┘──→│ Server2 │   │        │          │
│                              └─────────┘   └────────┘          │
│                                                                  │
│   ইন্টারনেট → রিভার্স প্রক্সি → সার্ভার                         │
│   (ক্লায়েন্ট জানে না আসল সার্ভার কোনটা)                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 🛡️ রিভার্স প্রক্সির কাজ

### ১. লোড ব্যালান্সিং
```
               ┌──────────────────┐
               │  Reverse Proxy   │
               └────────┬─────────┘
                        │
         ┌──────────────┼──────────────┐
         ↓              ↓              ↓
   ┌──────────┐  ┌──────────┐  ┌──────────┐
   │ Server 1 │  │ Server 2 │  │ Server 3 │
   └──────────┘  └──────────┘  └──────────┘

ট্রাফিক বিভিন্ন সার্ভারে ভাগ করে দেয়।
```

### ২. SSL Termination
```
Client ─── HTTPS ──→ Reverse Proxy ─── HTTP ──→ Server
         (Encrypted)                 (Unencrypted)

সুবিধা:
- সার্ভারে SSL overhead নেই
- সেন্ট্রালাইজড সার্টিফিকেট ম্যানেজমেন্ট
```

### ৩. ক্যাশিং
```
           ┌─────────────────────────┐
           │    Reverse Proxy        │
           │   ┌───────────────┐     │
           │   │    Cache      │     │
           │   │ /img/logo.png │     │
           │   │ /css/style.css│     │
           │   └───────────────┘     │
           └───────────┬─────────────┘
                       │
                       ↓ (Cache miss only)
                 ┌──────────┐
                 │  Server  │
                 └──────────┘

স্ট্যাটিক কন্টেন্ট ক্যাশ করে দ্রুত সার্ভ করে।
```

### ৪. কম্প্রেশন
```
Server Response:     Proxy After Compression:
100 KB HTML    →    25 KB (gzip)
500 KB JSON    →    100 KB (gzip)

ব্যান্ডউইথ বাঁচে, দ্রুত লোড হয়।
```

### ৫. সিকিউরিটি
```
┌─────────────────────────────────────────────┐
│            Reverse Proxy                     │
│  ┌─────────────────────────────────────┐    │
│  │         Security Layer              │    │
│  │  ├── DDoS Protection               │    │
│  │  ├── Rate Limiting                 │    │
│  │  ├── WAF (Web App Firewall)        │    │
│  │  ├── IP Blacklisting               │    │
│  │  └── Bot Detection                 │    │
│  └─────────────────────────────────────┘    │
└─────────────────────────────────────────────┘

অ্যাটাক প্রক্সিতে আটকে যায়, সার্ভার সুরক্ষিত।
```

### ৬. সার্ভার অ্যাড্রেস গোপন
```
Client জানে: https://example.com
Client জানে না: 10.0.1.50:8080 (আসল সার্ভার)

সিকিউরিটি বাড়ে - সার্ভারে সরাসরি অ্যাটাক করা যায় না।
```

## 🔧 Nginx রিভার্স প্রক্সি কনফিগ

### বেসিক কনফিগারেশন
```nginx
server {
    listen 80;
    server_name example.com;
    
    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### SSL Termination
```nginx
server {
    listen 443 ssl;
    server_name example.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    location / {
        proxy_pass http://backend;
    }
}
```

### ক্যাশিং সেটআপ
```nginx
proxy_cache_path /var/cache/nginx levels=1:2 
                 keys_zone=my_cache:10m max_size=1g 
                 inactive=60m use_temp_path=off;

server {
    location /static/ {
        proxy_cache my_cache;
        proxy_cache_valid 200 1d;
        proxy_cache_use_stale error timeout http_500 http_502;
        proxy_pass http://backend;
    }
}
```

### Rate Limiting
```nginx
limit_req_zone $binary_remote_addr zone=mylimit:10m rate=10r/s;

server {
    location /api/ {
        limit_req zone=mylimit burst=20 nodelay;
        proxy_pass http://api_backend;
    }
}
```

## 🌐 জনপ্রিয় রিভার্স প্রক্সি

### ১. Nginx
```
সুবিধা:
✓ অত্যন্ত দ্রুত
✓ কম মেমরি ব্যবহার
✓ ওয়েব সার্ভার + রিভার্স প্রক্সি
✓ ফ্রি ও ওপেন সোর্স

ব্যবহার: ওয়েব অ্যাপ, API
```

### ২. HAProxy
```
সুবিধা:
✓ হাই পারফরম্যান্স
✓ TCP ও HTTP সাপোর্ট
✓ অ্যাডভান্সড হেলথ চেক
✓ ডিটেইলড স্ট্যাটিস্টিক্স

ব্যবহার: হাই-ট্রাফিক সাইট, TCP অ্যাপ
```

### ৩. Traefik
```
সুবিধা:
✓ ক্লাউড-নেটিভ
✓ অটো-ডিসকভারি
✓ Docker/Kubernetes ইন্টিগ্রেশন
✓ Let's Encrypt অটো-SSL

ব্যবহার: কন্টেইনার, মাইক্রোসার্ভিস
```

### ৪. Envoy
```
সুবিধা:
✓ সার্ভিস মেশ সাপোর্ট
✓ gRPC সাপোর্ট
✓ অবজার্ভেবিলিটি
✓ ডায়নামিক কনফিগ

ব্যবহার: Istio, সার্ভিস মেশ
```

## 📊 তুলনা

```
┌─────────────┬─────────────┬─────────────┬─────────────┐
│   Feature   │   Nginx     │  HAProxy    │  Traefik    │
├─────────────┼─────────────┼─────────────┼─────────────┤
│ Web Server  │    ✓        │    ✗        │    ✗        │
│ L4 LB       │    ✓        │    ✓        │    ✓        │
│ L7 LB       │    ✓        │    ✓        │    ✓        │
│ SSL Term    │    ✓        │    ✓        │    ✓        │
│ Caching     │    ✓        │    ✗        │    ✗        │
│ Auto-SSL    │   Plus      │    ✗        │    ✓        │
│ K8s Native  │    ✗        │    ✗        │    ✓        │
│ Config      │   Static    │  Static     │  Dynamic    │
└─────────────┴─────────────┴─────────────┴─────────────┘
```

## ✅ রিভার্স প্রক্সি চেকলিস্ট

- [ ] SSL সার্টিফিকেট কনফিগার
- [ ] ক্যাশিং সেটআপ
- [ ] কম্প্রেশন এনাবল
- [ ] হেলথ চেক কনফিগার
- [ ] লগিং সেটআপ
- [ ] Rate Limiting
- [ ] সিকিউরিটি হেডার
- [ ] মনিটরিং

## 📚 পরবর্তী সেকশন

[ক্যাশিং →](../04-caching/README.md)
