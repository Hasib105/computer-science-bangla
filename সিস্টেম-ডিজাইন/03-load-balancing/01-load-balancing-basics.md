# লোড ব্যালান্সিং বেসিক (Load Balancing Basics)

## 🎯 লোড ব্যালান্সিং কি?

**লোড ব্যালান্সার** হলো একটি সিস্টেম যা ইনকামিং ট্রাফিক একাধিক সার্ভারে ভাগ করে দেয়।

```
লোড ব্যালান্সার ছাড়া:
┌──────────┐
│  Users   │ ────────────→  ┌──────────┐
└──────────┘                │ Server 1 │ ← Overloaded!
                            └──────────┘

লোড ব্যালান্সার সহ:
                            ┌──────────┐
                       ┌──→ │ Server 1 │
┌──────────┐    ┌────────┐  └──────────┘
│  Users   │ →  │  Load  │  ┌──────────┐
└──────────┘    │Balancer│─→│ Server 2 │
                └────────┘  └──────────┘
                       └──→ ┌──────────┐
                            │ Server 3 │
                            └──────────┘
```

## 📊 কেন লোড ব্যালান্সিং দরকার?

### ১. ট্রাফিক ডিস্ট্রিবিউশন
```
প্রতি সেকেন্ডে ৩০০০ রিকোয়েস্ট

এক সার্ভার: ৩০০০ RPS (ক্র্যাশ!)
তিন সার্ভার: ১০০০ RPS প্রতিটি (ঠিক আছে ✓)
```

### ২. High Availability
```
সার্ভার ফেইলওভার:

Server 1: ✗ (Dead)
Server 2: ✓ (Running)
Server 3: ✓ (Running)

লোড ব্যালান্সার → Server 1 এ ট্রাফিক পাঠায় না
               → Server 2, 3 এ পাঠায়
```

### ৩. ফ্লেক্সিবল স্কেলিং
```
লোড কম:                    লোড বেশি:
┌──────────┐               ┌──────────┐
│ Server 1 │               │ Server 1 │
└──────────┘               ├──────────┤
                           │ Server 2 │
                           ├──────────┤
                           │ Server 3 │
                           ├──────────┤
                           │ Server 4 │
                           └──────────┘

প্রয়োজন অনুযায়ী সার্ভার যোগ/বাদ দেওয়া যায়
```

## 🏗️ লোড ব্যালান্সার আর্কিটেকচার

### সাধারণ আর্কিটেকচার
```
                    Internet
                        │
                        ↓
              ┌──────────────────┐
              │   DNS Server     │
              │ example.com      │
              │ → LB IP Address  │
              └────────┬─────────┘
                       │
                       ↓
              ┌──────────────────┐
              │  Load Balancer   │
              │  (Public IP)     │
              └────────┬─────────┘
                       │
         ┌─────────────┼─────────────┐
         ↓             ↓             ↓
   ┌──────────┐  ┌──────────┐  ┌──────────┐
   │ Server 1 │  │ Server 2 │  │ Server 3 │
   │(Private) │  │(Private) │  │(Private) │
   └──────────┘  └──────────┘  └──────────┘
```

### মাল্টি-টায়ার আর্কিটেকচার
```
                    Internet
                        │
                        ↓
              ┌──────────────────┐
              │   L4 Load        │ ← TCP/UDP লেভেল
              │   Balancer       │
              └────────┬─────────┘
                       │
         ┌─────────────┼─────────────┐
         ↓             ↓             ↓
   ┌──────────┐  ┌──────────┐  ┌──────────┐
   │    L7    │  │    L7    │  │    L7    │ ← HTTP লেভেল
   │    LB    │  │    LB    │  │    LB    │
   └────┬─────┘  └────┬─────┘  └────┬─────┘
        │             │             │
   ┌────┴────┐   ┌────┴────┐   ┌────┴────┐
   │ Servers │   │ Servers │   │ Servers │
   └─────────┘   └─────────┘   └─────────┘
```

## 🔍 হেলথ চেক

### হেলথ চেক কি?
লোড ব্যালান্সার নিয়মিত চেক করে সার্ভার চালু আছে কিনা।

```
Health Check Types:

১. TCP Health Check
   - পোর্টে কানেক্ট হয় কিনা
   - সিম্পল, দ্রুত

২. HTTP Health Check
   - GET /health → 200 OK?
   - অ্যাপ্লিকেশন লেভেল চেক

৩. Deep Health Check
   - GET /health/deep
   - ডাটাবেস, ক্যাশ সব চেক করে
```

### হেলথ চেক কনফিগারেশন (Nginx)
```nginx
upstream backend {
    server server1:8080;
    server server2:8080;
    server server3:8080;
    
    # Health check settings
    health_check interval=5s fails=3 passes=2;
}

server {
    location / {
        proxy_pass http://backend;
    }
}
```

### হেলথ চেক কনফিগারেশন (HAProxy)
```haproxy
backend servers
    balance roundrobin
    option httpchk GET /health
    http-check expect status 200
    
    server server1 10.0.0.1:8080 check inter 5s fall 3 rise 2
    server server2 10.0.0.2:8080 check inter 5s fall 3 rise 2
    server server3 10.0.0.3:8080 check inter 5s fall 3 rise 2
```

## 🔄 সেশন পার্সিস্টেন্স (Sticky Sessions)

### সমস্যা
```
Request 1 → Server 1 (সেশন তৈরি)
Request 2 → Server 2 (সেশন নেই!)
Request 3 → Server 3 (সেশন নেই!)
```

### সমাধান ১: Sticky Sessions
```
                    ┌──────────────────┐
                    │  Load Balancer   │
                    │ User A → Server 1│
                    │ User B → Server 2│
                    └────────┬─────────┘

User A-এর সব রিকোয়েস্ট Server 1-এ যাবে

পদ্ধতি:
- Cookie-based (JSESSIONID, PHPSESSID)
- IP-based (Source IP hash)
```

### সমাধান ২: Shared Session Store (Better)
```
┌──────────┐  ┌──────────┐  ┌──────────┐
│ Server 1 │  │ Server 2 │  │ Server 3 │
└────┬─────┘  └────┬─────┘  └────┬─────┘
     │             │             │
     └─────────────┼─────────────┘
                   ↓
           ┌──────────────┐
           │    Redis     │
           │ Session Store│
           └──────────────┘

যেকোনো সার্ভার সেশন পড়তে পারে।
```

## 🛡️ SSL Termination

### SSL Termination কি?
```
SSL Termination at Load Balancer:

Client ─── HTTPS ──→ Load Balancer ─── HTTP ──→ Servers
         (Encrypted)                  (Unencrypted)

সুবিধা:
✓ সার্ভারে SSL overhead নেই
✓ সেন্ট্রালাইজড সার্টিফিকেট ম্যানেজমেন্ট
✓ ইজি ডিবাগিং

অসুবিধা:
✗ LB ও সার্ভারের মধ্যে আনএনক্রিপ্টেড
```

### End-to-End SSL
```
Client ─── HTTPS ──→ Load Balancer ─── HTTPS ──→ Servers
         (Encrypted)                 (Encrypted)

সব জায়গায় এনক্রিপ্টেড - বেশি সিকিউর
কিন্তু বেশি CPU overhead
```

## ☁️ ক্লাউড লোড ব্যালান্সার

### AWS
```
┌─────────────────────────────────────────────────────────┐
│                     AWS Load Balancers                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Classic LB (CLB)     - লিগ্যাসি, L4 + L7              │
│  Application LB (ALB) - L7, HTTP/HTTPS, WebSocket      │
│  Network LB (NLB)     - L4, High Performance, TCP/UDP  │
│  Gateway LB (GWLB)    - L3, Network Appliances         │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Google Cloud
```
- Global HTTP(S) Load Balancer
- Regional Internal Load Balancer
- Network Load Balancer
```

### Azure
```
- Azure Load Balancer (L4)
- Application Gateway (L7)
- Front Door (Global L7)
```

## 📊 লোড ব্যালান্সার তুলনা

```
┌─────────────┬──────────────┬──────────────┬──────────────┐
│    Tool     │    Type      │     Use      │   Features   │
├─────────────┼──────────────┼──────────────┼──────────────┤
│   Nginx     │   L7         │  Web Server  │ HTTP, SSL,   │
│             │              │  + LB        │ Caching      │
├─────────────┼──────────────┼──────────────┼──────────────┤
│   HAProxy   │   L4/L7      │  Dedicated   │ High Perf,   │
│             │              │  LB          │ TCP/HTTP     │
├─────────────┼──────────────┼──────────────┼──────────────┤
│   Traefik   │   L7         │  Cloud-      │ Auto-        │
│             │              │  Native      │ discovery    │
├─────────────┼──────────────┼──────────────┼──────────────┤
│   Envoy     │   L4/L7      │  Service     │ gRPC,        │
│             │              │  Mesh        │ Observability│
└─────────────┴──────────────┴──────────────┴──────────────┘
```

## ✅ লোড ব্যালান্সিং চেকলিস্ট

- [ ] সঠিক অ্যালগরিদম নির্বাচন
- [ ] হেলথ চেক কনফিগার
- [ ] SSL সেটআপ
- [ ] সেশন হ্যান্ডলিং
- [ ] মনিটরিং ও লগিং
- [ ] ফেইলওভার টেস্টিং
- [ ] ক্যাপাসিটি প্ল্যানিং

## 📚 পরবর্তী টপিক

[লোড ব্যালান্সিং অ্যালগরিদম →](./02-algorithms.md)
