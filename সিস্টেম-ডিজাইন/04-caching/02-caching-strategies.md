# ক্যাশিং স্ট্র্যাটেজি (Caching Strategies)

## 🎯 প্রধান ক্যাশিং প্যাটার্ন

```
┌─────────────────────────────────────────────────────────────────┐
│                   ক্যাশিং স্ট্র্যাটেজি                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Read Strategies:                                              │
│  ├── Cache-Aside (Lazy Loading)                                │
│  └── Read-Through                                              │
│                                                                 │
│  Write Strategies:                                             │
│  ├── Write-Through                                             │
│  ├── Write-Behind (Write-Back)                                 │
│  └── Write-Around                                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 📖 ১. Cache-Aside (Lazy Loading)

### কিভাবে কাজ করে?
```
অ্যাপ্লিকেশন নিজে ক্যাশ ম্যানেজ করে।

Read Flow:
┌──────────┐    ┌─────────┐    ┌──────────┐
│   App    │ ──→│  Cache  │    │    DB    │
└──────────┘    └─────────┘    └──────────┘
     │               │              │
     │─── 1. Get ───→│              │
     │←── 2. Miss ───│              │
     │───────── 3. Query ─────────→│
     │←──────── 4. Data ───────────│
     │─── 5. Set ───→│              │
     │←── 6. Data ───│              │

Cache Hit হলে ধাপ ৩-৫ বাদ।
```

### কোড উদাহরণ
```python
def get_user(user_id):
    cache_key = f"user:{user_id}"
    
    # 1. ক্যাশে চেক
    user = cache.get(cache_key)
    
    if user is None:  # 2. Cache Miss
        # 3. ডাটাবেস থেকে পড়া
        user = db.query("SELECT * FROM users WHERE id = ?", user_id)
        
        if user:
            # 5. ক্যাশে রাখা
            cache.set(cache_key, user, ttl=3600)
    
    return user

def update_user(user_id, data):
    # ডাটাবেস আপডেট
    db.update("UPDATE users SET ... WHERE id = ?", user_id)
    
    # ক্যাশ ইনভ্যালিডেট
    cache.delete(f"user:{user_id}")
```

### সুবিধা ও অসুবিধা
```
সুবিধা:
✓ সিম্পল ইমপ্লিমেন্টেশন
✓ শুধু প্রয়োজনীয় ডাটা ক্যাশ হয়
✓ ক্যাশ ফেইলারে সিস্টেম চলে

অসুবিধা:
✗ প্রথম রিকোয়েস্ট স্লো (Cache Miss)
✗ ক্যাশ ও DB আনসিঙ্ক হতে পারে
✗ Thundering Herd সমস্যা
```

## 📖 ২. Read-Through

### কিভাবে কাজ করে?
```
ক্যাশ নিজে DB থেকে ডাটা লোড করে।

Read Flow:
┌──────────┐    ┌─────────┐    ┌──────────┐
│   App    │ ──→│  Cache  │ ──→│    DB    │
└──────────┘    └─────────┘    └──────────┘
     │               │              │
     │─── 1. Get ───→│              │
     │               │─── 2. Get ──→│ (if miss)
     │               │←── 3. Data ──│
     │←── 4. Data ───│              │

অ্যাপ সরাসরি DB-তে যায় না।
```

### কোড উদাহরণ
```python
# Read-Through ক্যাশ সাধারণত লাইব্রেরি দিয়ে করা হয়
# যেমন: Spring Cache, cache-aside with loader

@cache_result(ttl=3600)
def get_user(user_id):
    return db.query("SELECT * FROM users WHERE id = ?", user_id)
```

### সুবিধা ও অসুবিধা
```
সুবিধা:
✓ অ্যাপ কোড সিম্পল
✓ ক্যাশ লজিক আলাদা

অসুবিধা:
✗ ক্যাশ লেয়ার জটিল
✗ প্রথম রিকোয়েস্ট স্লো
```

## ✏️ ৩. Write-Through

### কিভাবে কাজ করে?
```
ক্যাশ ও DB দুইটাতেই একসাথে লেখা হয়।

Write Flow:
┌──────────┐    ┌─────────┐    ┌──────────┐
│   App    │ ──→│  Cache  │ ──→│    DB    │
└──────────┘    └─────────┘    └──────────┘
     │               │              │
     │── 1. Write ──→│              │
     │               │── 2. Write ─→│
     │               │←── 3. Ack ───│
     │←── 4. Ack ────│              │

DB-তে সফলভাবে লেখা হলেই Ack পায়।
```

### কোড উদাহরণ
```python
def update_user(user_id, data):
    cache_key = f"user:{user_id}"
    
    # ক্যাশে লেখা
    cache.set(cache_key, data)
    
    # DB-তে লেখা
    db.update("UPDATE users SET ... WHERE id = ?", user_id)
    
    return True
```

### সুবিধা ও অসুবিধা
```
সুবিধা:
✓ ক্যাশ সবসময় আপডেটেড
✓ কোনো স্টেল ডাটা নেই
✓ রিড ফাস্ট (সবসময় ক্যাশ হিট)

অসুবিধা:
✗ রাইট স্লো (দুই জায়গায় লিখতে হয়)
✗ যে ডাটা পড়া হয় না সেটাও ক্যাশে থাকে
```

## ✏️ ৪. Write-Behind (Write-Back)

### কিভাবে কাজ করে?
```
প্রথমে ক্যাশে লেখা, পরে অ্যাসিনক্রোনাসলি DB-তে।

Write Flow:
┌──────────┐    ┌─────────┐         ┌──────────┐
│   App    │ ──→│  Cache  │ - - - → │    DB    │
└──────────┘    └─────────┘  async  └──────────┘
     │               │                   │
     │── 1. Write ──→│                   │
     │←── 2. Ack ────│                   │
     │               │─── 3. Later ────→│
     │               │      Batch Write  │

দ্রুত Ack, পরে ব্যাচে DB-তে লেখা।
```

### সুবিধা ও অসুবিধা
```
সুবিধা:
✓ খুব দ্রুত রাইট
✓ ব্যাচিং করে DB লোড কমায়
✓ হাই থ্রুপুট

অসুবিধা:
✗ ডাটা লস রিস্ক (ক্যাশ ক্র্যাশ হলে)
✗ জটিল ইমপ্লিমেন্টেশন
✗ Eventual Consistency
```

## ✏️ ৫. Write-Around

### কিভাবে কাজ করে?
```
সরাসরি DB-তে লেখা, ক্যাশ আপডেট করা হয় না।

Write Flow:
┌──────────┐    ┌─────────┐    ┌──────────┐
│   App    │    │  Cache  │    │    DB    │
└──────────┘    └─────────┘    └──────────┘
     │               │              │
     │─────── 1. Write ───────────→│
     │←────── 2. Ack ─────────────│

পরবর্তী রিডে ক্যাশ মিস হবে, তখন ক্যাশ হবে।
```

### সুবিধা ও অসুবিধা
```
সুবিধা:
✓ ক্যাশ পলিউশন কম
✓ একবার-পড়া ডাটা ক্যাশে থাকে না

অসুবিধা:
✗ রাইটের পর রিড স্লো (ক্যাশ মিস)
```

## 📊 স্ট্র্যাটেজি তুলনা

```
┌─────────────────┬─────────────┬─────────────┬─────────────────────┐
│    Strategy     │  Read Speed │ Write Speed │       Use Case      │
├─────────────────┼─────────────┼─────────────┼─────────────────────┤
│ Cache-Aside     │    Fast     │   Medium    │ সাধারণ ব্যবহার      │
│ Read-Through    │    Fast     │   Medium    │ অটো ক্যাশ লোডিং    │
│ Write-Through   │    Fast     │    Slow     │ কনসিস্টেন্সি গুরুত্বপূর্ণ│
│ Write-Behind    │    Fast     │    Fast     │ হাই থ্রুপুট রাইট   │
│ Write-Around    │   Medium    │    Fast     │ একবার-লেখা ডাটা   │
└─────────────────┴─────────────┴─────────────┴─────────────────────┘
```

## 🔀 কম্বাইন্ড স্ট্র্যাটেজি

### Cache-Aside + Write-Through
```
Read: Cache-Aside
Write: Write-Through

সবচেয়ে সাধারণ কম্বিনেশন।
```

### Read-Through + Write-Behind
```
Read: Read-Through
Write: Write-Behind

হাই পারফরম্যান্সের জন্য।
```

## ⚡ Thundering Herd সমস্যা

### সমস্যা
```
Cache Key মেয়াদ উত্তীর্ণ হলে:

t=0: Key expires
t=1: Request 1 → Cache Miss → DB Query
t=1: Request 2 → Cache Miss → DB Query
t=1: Request 3 → Cache Miss → DB Query
...
t=1: Request 1000 → Cache Miss → DB Query

১০০০ রিকোয়েস্ট একসাথে DB-তে!
```

### সমাধান

#### ১. Locking
```python
def get_user(user_id):
    cache_key = f"user:{user_id}"
    lock_key = f"lock:{cache_key}"
    
    user = cache.get(cache_key)
    
    if user is None:
        # Lock নেওয়ার চেষ্টা
        if cache.setnx(lock_key, 1, ttl=10):
            try:
                user = db.query(...)
                cache.set(cache_key, user, ttl=3600)
            finally:
                cache.delete(lock_key)
        else:
            # অন্যরা অপেক্ষা করবে
            time.sleep(0.1)
            return get_user(user_id)
    
    return user
```

#### ২. Probabilistic Early Expiration
```python
def get_with_early_recompute(key, ttl=3600, beta=1):
    data, expiry = cache.get_with_expiry(key)
    
    # Probabilistically refresh before expiry
    if data and random() < beta * (now() / expiry):
        # Background refresh
        async_refresh(key)
    
    return data
```

## 💡 কোন স্ট্র্যাটেজি বেছে নেবেন?

```
প্রশ্ন ১: রিড বেশি না রাইট?
├── রিড বেশি → Cache-Aside বা Read-Through
└── রাইট বেশি → Write-Behind

প্রশ্ন ২: কনসিস্টেন্সি কতটা গুরুত্বপূর্ণ?
├── খুব গুরুত্বপূর্ণ → Write-Through
└── Eventual OK → Write-Behind বা Cache-Aside

প্রশ্ন ৩: ডাটা লস সহ্য করা যাবে?
├── না → Write-Through
└── হ্যাঁ → Write-Behind

প্রশ্ন ৪: সিস্টেম জটিলতা সীমা?
├── সিম্পল রাখতে চাই → Cache-Aside
└── জটিলতা ঠিক আছে → Read-Through + Write-Behind
```

## 📚 পরবর্তী টপিক

[ক্যাশ ইনভ্যালিডেশন →](./03-cache-invalidation.md)
