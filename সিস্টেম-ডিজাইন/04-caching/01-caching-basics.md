# ক্যাশিং বেসিক (Caching Basics)

## 🎯 ক্যাশিং কি?

**ক্যাশিং** হলো ঘন ঘন ব্যবহৃত ডাটা একটি দ্রুত স্টোরেজে রেখে দেওয়া যাতে পরবর্তী বার দ্রুত অ্যাক্সেস করা যায়।

```
ক্যাশ ছাড়া:
Request → Server → Database (১০০ ms)
Request → Server → Database (১০০ ms)
Request → Server → Database (১০০ ms)

ক্যাশ সহ:
Request → Server → Cache (১ ms) ✓ Cache Hit
Request → Server → Cache (১ ms) ✓ Cache Hit
Request → Server → Database → Cache (১০০ ms) ✗ Cache Miss
Request → Server → Cache (১ ms) ✓ Cache Hit
```

## 📊 ক্যাশিং কেন দরকার?

### ১. লেটেন্সি কমানো
```
Database Query: ১০০ ms
Cache Read: ১ ms

১০০x দ্রুত!
```

### ২. ডাটাবেস লোড কমানো
```
প্রতি সেকেন্ডে ১০,০০০ রিকোয়েস্ট

ক্যাশ হিট রেট ৯৫%:
├── ক্যাশ থেকে: ৯,৫০০ রিকোয়েস্ট
└── ডাটাবেস থেকে: ৫০০ রিকোয়েস্ট

ডাটাবেস লোড ৯৫% কমে গেল!
```

### ৩. খরচ বাঁচানো
```
ডাটাবেস ক্যাপাসিটি:
ক্যাশ ছাড়া: ১০ সার্ভার দরকার
ক্যাশ সহ: ২ সার্ভার যথেষ্ট

৮০% খরচ বাঁচলো!
```

## 🏗️ ক্যাশিং লেয়ার

```
┌─────────────────────────────────────────────────────────────────┐
│                      ক্যাশিং লেয়ার                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐                                               │
│  │   Browser   │ ← Browser Cache (Local)                       │
│  └──────┬──────┘                                               │
│         │                                                       │
│  ┌──────┴──────┐                                               │
│  │     CDN     │ ← CDN Cache (Edge)                            │
│  └──────┬──────┘                                               │
│         │                                                       │
│  ┌──────┴──────┐                                               │
│  │   Nginx     │ ← Reverse Proxy Cache                         │
│  └──────┬──────┘                                               │
│         │                                                       │
│  ┌──────┴──────┐                                               │
│  │Application │ ← Application Cache (Redis/Memcached)         │
│  └──────┬──────┘                                               │
│         │                                                       │
│  ┌──────┴──────┐                                               │
│  │  Database   │ ← Database Query Cache                        │
│  └─────────────┘                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### ১. ব্রাউজার ক্যাশ
```
HTTP Headers:
Cache-Control: max-age=86400
Expires: Thu, 01 Jan 2026 00:00:00 GMT
ETag: "abc123"

ব্রাউজার লোকালি স্টোর করে।
JS, CSS, Images এর জন্য ভালো।
```

### ২. CDN ক্যাশ
```
                      ┌─────────┐
                      │  Origin │
                      │  Server │
                      └────┬────┘
                           │
        ┌──────────────────┼──────────────────┐
        ↓                  ↓                  ↓
  ┌───────────┐      ┌───────────┐      ┌───────────┐
  │ CDN Edge  │      │ CDN Edge  │      │ CDN Edge  │
  │ (Asia)    │      │ (Europe)  │      │ (America) │
  └───────────┘      └───────────┘      └───────────┘

ইউজারের কাছের Edge থেকে কন্টেন্ট সার্ভ হয়।
```

### ৩. অ্যাপ্লিকেশন ক্যাশ (Redis/Memcached)
```
def get_user(user_id):
    # ক্যাশে চেক করো
    user = cache.get(f"user:{user_id}")
    
    if user is None:  # Cache Miss
        # ডাটাবেস থেকে পড়ো
        user = db.query(f"SELECT * FROM users WHERE id = {user_id}")
        # ক্যাশে রাখো
        cache.set(f"user:{user_id}", user, ttl=3600)
    
    return user
```

### ৪. ডাটাবেস ক্যাশ
```
MySQL Query Cache (Deprecated in 8.0)
PostgreSQL pg_prewarm

সাধারণত অ্যাপ্লিকেশন ক্যাশ ভালো অপশন।
```

## 📈 ক্যাশ মেট্রিক্স

### ক্যাশ হিট রেট
```
Cache Hit Rate = (Cache Hits / Total Requests) × ১০০%

উদাহরণ:
Total Requests: ১০,০০০
Cache Hits: ৯,৫০০
Cache Misses: ৫০০

Hit Rate = (৯,৫০০ / ১০,০০০) × ১০০% = ৯৫%

টার্গেট: ৯০%+ হিট রেট
```

### ক্যাশ মিসের প্রকার
```
১. Cold Miss (Compulsory Miss)
   - প্রথম বার ডাটা অ্যাক্সেস
   - এড়ানো সম্ভব না

২. Capacity Miss
   - ক্যাশ ফুল, পুরাতন ডাটা বাদ দিতে হয়েছে
   - সমাধান: ক্যাশ সাইজ বাড়ানো

৩. Conflict Miss
   - ক্যাশ এন্ট্রি রিপ্লেস হয়ে গেছে
   - সমাধান: বেটার ইভিকশন পলিসি
```

## 🔄 ক্যাশ ইভিকশন পলিসি

ক্যাশ ফুল হলে কোন ডাটা বাদ দেবে?

### ১. LRU (Least Recently Used)
```
সবচেয়ে পুরাতন অ্যাক্সেস হওয়া আইটেম বাদ।

Access Order: A, B, C, D, E (cache size = 3)
Cache: [C, D, E]
Access F: Cache: [D, E, F] (C বাদ)

সবচেয়ে জনপ্রিয় পলিসি।
```

### ২. LFU (Least Frequently Used)
```
সবচেয়ে কম অ্যাক্সেস হওয়া আইটেম বাদ।

Access Count:
A: ১০ বার
B: ২ বার
C: ৫ বার

B বাদ হবে।
```

### ৩. FIFO (First In First Out)
```
প্রথমে আসা আইটেম প্রথমে বাদ।

Queue: [A, B, C]
Add D: [B, C, D] (A বাদ)
```

### ৪. Random
```
র‍্যান্ডমলি বাদ দেওয়া।

সিম্পল, কিন্তু কম কার্যকর।
```

### ৫. TTL (Time To Live)
```
নির্দিষ্ট সময় পর অটো-এক্সপায়ার।

cache.set("user:123", data, ttl=3600)  # ১ ঘন্টা
```

## 💾 ক্যাশ স্টোরেজ প্রকার

### ১. In-Memory Cache
```
RAM-এ স্টোর করা।

সুবিধা:
✓ অত্যন্ত দ্রুত (μs লেভেল)
✓ সিম্পল

অসুবিধা:
✗ সীমিত সাইজ
✗ সার্ভার রিস্টার্টে হারিয়ে যায়

উদাহরণ: Redis, Memcached
```

### ২. Disk Cache
```
SSD/HDD-তে স্টোর করা।

সুবিধা:
✓ বড় সাইজ সম্ভব
✓ পার্সিস্টেন্ট

অসুবিধা:
✗ RAM এর চেয়ে স্লো

উদাহরণ: Varnish, Nginx cache
```

### ৩. Distributed Cache
```
একাধিক সার্ভারে ছড়িয়ে।

     ┌─────────┐
     │  App    │
     └────┬────┘
          │
    ┌─────┴─────┐
    ↓           ↓
┌───────┐  ┌───────┐  ┌───────┐
│Redis 1│  │Redis 2│  │Redis 3│
└───────┘  └───────┘  └───────┘

সুবিধা:
✓ হাই অ্যাভেইলেবিলিটি
✓ স্কেলেবল

অসুবিধা:
✗ জটিল
✗ নেটওয়ার্ক লেটেন্সি
```

## 🎯 কি ক্যাশ করবেন?

### ক্যাশ করা উচিত
```
✓ ঘন ঘন পড়া হয় এমন ডাটা
✓ কমপিউট করতে সময় লাগে এমন ডাটা
✓ পরিবর্তন কম হয় এমন ডাটা
✓ স্ট্যাটিক কন্টেন্ট

উদাহরণ:
- ইউজার প্রোফাইল
- প্রোডাক্ট ক্যাটালগ
- কনফিগারেশন
- সেশন ডাটা
- API রেসপন্স
```

### ক্যাশ করা উচিত না
```
✗ ঘন ঘন পরিবর্তন হয় এমন ডাটা
✗ ইউজার-স্পেসিফিক সেনসিটিভ ডাটা
✗ রিয়েল-টাইম ডাটা
✗ বড় ফাইল (সাধারণত)

উদাহরণ:
- স্টক প্রাইস (রিয়েল-টাইম)
- চ্যাট মেসেজ
- পাসওয়ার্ড
```

## 📊 ক্যাশিং উদাহরণ

### Python + Redis
```python
import redis
import json

cache = redis.Redis(host='localhost', port=6379)

def get_user(user_id):
    cache_key = f"user:{user_id}"
    
    # ক্যাশে চেক
    cached = cache.get(cache_key)
    if cached:
        return json.loads(cached)
    
    # ডাটাবেস থেকে পড়া
    user = db.query(f"SELECT * FROM users WHERE id = {user_id}")
    
    # ক্যাশে রাখা (১ ঘন্টার জন্য)
    cache.setex(cache_key, 3600, json.dumps(user))
    
    return user
```

## ✅ ক্যাশিং চেকলিস্ট

- [ ] কোন ডাটা ক্যাশ করবেন নির্ধারণ করুন
- [ ] সঠিক TTL নির্বাচন করুন
- [ ] ইভিকশন পলিসি সেট করুন
- [ ] ক্যাশ হিট রেট মনিটর করুন
- [ ] ক্যাশ ইনভ্যালিডেশন স্ট্র্যাটেজি
- [ ] ফেইলওভার হ্যান্ডলিং

## 📚 পরবর্তী টপিক

[ক্যাশিং স্ট্র্যাটেজি →](./02-caching-strategies.md)
