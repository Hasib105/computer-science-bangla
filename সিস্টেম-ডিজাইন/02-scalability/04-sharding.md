# শার্ডিং (Sharding)

## 🎯 শার্ডিং কি?

**শার্ডিং** হলো ডাটাকে একাধিক ডাটাবেস সার্ভারে ভাগ করে রাখার টেকনিক। প্রতিটি ভাগকে বলা হয় **শার্ড**।

```
একটি বড় ডাটাবেস → একাধিক ছোট ডাটাবেস (শার্ড)

┌──────────────────────────────────┐
│        বড় ডাটাবেস               │
│      100 Million Users          │
└──────────────────────────────────┘
                │
                ↓ শার্ডিং
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│ Shard 1 │ │ Shard 2 │ │ Shard 3 │ │ Shard 4 │
│  25M    │ │  25M    │ │  25M    │ │  25M    │
└─────────┘ └─────────┘ └─────────┘ └─────────┘
```

## 📊 কেন শার্ডিং করবেন?

### সমস্যা
```
সিঙ্গেল ডাটাবেসের সীমাবদ্ধতা:
- ডিস্ক স্পেস শেষ
- RAM-এ পুরো ইনডেক্স রাখা যাচ্ছে না
- CPU ওভারলোড
- নেটওয়ার্ক ব্যান্ডউইথ সীমা
- ব্যাকআপ অনেক সময় নেয়
```

### সমাধান: শার্ডিং
```
প্রতিটি শার্ড:
✓ ছোট ডাটাসেট
✓ দ্রুত কুয়েরি
✓ সহজ ব্যাকআপ
✓ আলাদাভাবে স্কেল করা যায়
```

## 🔑 শার্ড কী (Shard Key)

### শার্ড কী কি?
শার্ড কী নির্ধারণ করে কোন ডাটা কোন শার্ডে যাবে।

```
উদাহরণ: user_id দিয়ে শার্ডিং

shard_number = hash(user_id) % total_shards

user_id = 12345
hash(12345) = 567890
567890 % 4 = 2
→ এই ইউজার Shard 2-তে থাকবে
```

### ভালো শার্ড কী বৈশিষ্ট্য
```
✓ হাই কার্ডিনালিটি (অনেক ইউনিক ভ্যালু)
✓ সমান ডিস্ট্রিবিউশন
✓ ইমমিউটেবল (পরিবর্তন হয় না)
✓ ঘন ঘন কুয়েরিতে ব্যবহার হয়
```

### ভালো vs খারাপ শার্ড কী
```
ভালো শার্ড কী:
├── user_id
├── order_id
└── device_id

খারাপ শার্ড কী:
├── country (কম কার্ডিনালিটি)
├── status (active/inactive)
└── created_date (হটস্পট)
```

## 🔪 শার্ডিং স্ট্র্যাটেজি

### ১. হ্যাশ-বেসড শার্ডিং
```
shard_id = hash(shard_key) % num_shards

┌──────────────────────────────────────────────────────────┐
│                   Hash Function                          │
│  user_id → hash() → shard_id                            │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  user_1  → hash(1)  = 4521 → 4521 % 4 = 1 → Shard 1    │
│  user_2  → hash(2)  = 8734 → 8734 % 4 = 2 → Shard 2    │
│  user_3  → hash(3)  = 1256 → 1256 % 4 = 0 → Shard 0    │
│  user_4  → hash(4)  = 9999 → 9999 % 4 = 3 → Shard 3    │
│                                                          │
└──────────────────────────────────────────────────────────┘

সুবিধা:
✓ সমান ডিস্ট্রিবিউশন
✓ সিম্পল ইমপ্লিমেন্টেশন

অসুবিধা:
✗ রেঞ্জ কুয়েরি কঠিন
✗ রিশার্ডিং জটিল
```

### ২. রেঞ্জ-বেসড শার্ডিং
```
┌──────────────────────────────────────────────────────────┐
│                   Range Based                            │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Shard 0: user_id 1 - 1,000,000                         │
│  Shard 1: user_id 1,000,001 - 2,000,000                │
│  Shard 2: user_id 2,000,001 - 3,000,000                │
│  Shard 3: user_id 3,000,001 - 4,000,000                │
│                                                          │
└──────────────────────────────────────────────────────────┘

সুবিধা:
✓ রেঞ্জ কুয়েরি সহজ
✓ সিম্পল লজিক

অসুবিধা:
✗ আনইভেন ডিস্ট্রিবিউশন (নতুন ডাটা শেষ শার্ডে)
✗ হটস্পট সমস্যা
```

### ৩. জিও-বেসড শার্ডিং
```
┌──────────────────────────────────────────────────────────┐
│                Geographic Sharding                       │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Shard Asia     : সব এশিয়ান ইউজার                       │
│  Shard Europe   : সব ইউরোপিয়ান ইউজার                    │
│  Shard Americas : সব আমেরিকান ইউজার                      │
│                                                          │
│      ┌─────────────────────────────────────────┐        │
│      │                 🌍                       │        │
│      │     🇪🇺 Europe    🇧🇩 Asia             │        │
│      │       Shard EU     Shard Asia           │        │
│      │                                         │        │
│      │     🇺🇸 Americas                        │        │
│      │       Shard US                          │        │
│      └─────────────────────────────────────────┘        │
│                                                          │
└──────────────────────────────────────────────────────────┘

সুবিধা:
✓ লো লেটেন্সি (লোকাল সার্ভার)
✓ ডাটা লোকালিটি রেগুলেশন মানা সহজ

অসুবিধা:
✗ আনইভেন লোড (ইউজার সংখ্যা এলাকা অনুযায়ী ভিন্ন)
✗ ক্রস-রিজিওন কুয়েরি জটিল
```

### ৪. ডিরেক্টরি-বেসড শার্ডিং
```
┌─────────────────────────────────────────────────────────┐
│                 Lookup Service                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │  user_id  │  shard_id                           │   │
│  ├───────────┼────────────────────────────────────┤   │
│  │  12345    │  shard_2                            │   │
│  │  67890    │  shard_1                            │   │
│  │  11111    │  shard_3                            │   │
│  └───────────┴────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         ↓               ↓               ↓
   ┌─────────┐     ┌─────────┐     ┌─────────┐
   │ Shard 1 │     │ Shard 2 │     │ Shard 3 │
   └─────────┘     └─────────┘     └─────────┘

সুবিধা:
✓ সবচেয়ে ফ্লেক্সিবল
✓ সহজে রিশার্ডিং

অসুবিধা:
✗ Lookup সার্ভিস বটলনেক
✗ বাড়তি জটিলতা
```

## ⚠️ শার্ডিং চ্যালেঞ্জ

### ১. Cross-Shard Queries
```
সমস্যা:
"সব ইউজার যাদের বয়স > ২৫"
→ সব শার্ডে কুয়েরি করতে হবে

সমাধান:
- Scatter-Gather প্যাটার্ন ব্যবহার
- অথবা ডাটা ডেনরমালাইজেশন
```

### ২. Joins
```
সমস্যা:
users টেবিল Shard 1-এ
orders টেবিল Shard 2-এ
JOIN কিভাবে করবেন?

সমাধান:
- অ্যাপ্লিকেশন-লেভেল JOIN
- ডাটা ডেনরমালাইজেশন
- একই শার্ড কী ব্যবহার (co-location)
```

### ৩. Transactions
```
সমস্যা:
একটি ট্রানজ্যাকশন দুই শার্ড জুড়ে

সমাধান:
- Two-Phase Commit (2PC)
- Saga Pattern
- Eventual Consistency গ্রহণ করা
```

### ৪. Resharding
```
সমস্যা:
৪ শার্ড থেকে ৮ শার্ডে যেতে হবে
সব ডাটা রি-ডিস্ট্রিবিউট করতে হবে

সমাধান:
- Consistent Hashing ব্যবহার
- ধীরে ধীরে মাইগ্রেশন
- Virtual Shards
```

## 🔄 Consistent Hashing

### সাধারণ হ্যাশিং সমস্যা
```
hash(key) % 4 = shard_id

শার্ড ৫ যোগ করলে:
hash(key) % 5 = নতুন shard_id

প্রায় সব ডাটা রি-ডিস্ট্রিবিউট হবে!
```

### Consistent Hashing সমাধান
```
          0°
          │
    ┌─────┴─────┐
    │           │
  Shard 3     Shard 1
    │           │
    │  ← Key1   │
    │     Key2→ │
    │           │
  Shard 2     Shard 4
    │           │
    └─────┬─────┘
          │
         180°

কী সবসময় ক্লকওয়াইজ নিকটতম শার্ডে যায়।

শার্ড ৫ যোগ করলে:
শুধু সেই সেকশনের কী রি-ডিস্ট্রিবিউট হবে!
```

## 💡 শার্ডিং বাস্তবায়ন উদাহরণ

### Python কোড উদাহরণ
```python
import hashlib

class ShardRouter:
    def __init__(self, num_shards):
        self.num_shards = num_shards
        self.shards = {
            0: "postgres://shard0:5432/db",
            1: "postgres://shard1:5432/db",
            2: "postgres://shard2:5432/db",
            3: "postgres://shard3:5432/db",
        }
    
    def get_shard(self, shard_key):
        """শার্ড কী থেকে শার্ড বের করা"""
        hash_value = int(hashlib.md5(
            str(shard_key).encode()
        ).hexdigest(), 16)
        shard_id = hash_value % self.num_shards
        return self.shards[shard_id]
    
    def get_user(self, user_id):
        shard_url = self.get_shard(user_id)
        conn = connect(shard_url)
        return conn.query(
            "SELECT * FROM users WHERE id = ?", 
            user_id
        )

# ব্যবহার
router = ShardRouter(num_shards=4)
user = router.get_user(12345)
```

## ✅ শার্ডিং চেকলিস্ট

- [ ] সঠিক শার্ড কী নির্বাচন
- [ ] শার্ডিং স্ট্র্যাটেজি নির্ধারণ
- [ ] Cross-shard কুয়েরি হ্যান্ডলিং প্ল্যান
- [ ] ট্রানজ্যাকশন হ্যান্ডলিং
- [ ] মনিটরিং ও অ্যালার্টিং
- [ ] রিশার্ডিং প্ল্যান
- [ ] ব্যাকআপ স্ট্র্যাটেজি

## 📚 পরবর্তী টপিক

[রেপ্লিকেশন →](./05-replication.md)
