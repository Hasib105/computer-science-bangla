# Memory Management Basics (মেমরি ব্যবস্থাপনার মৌলিক ধারণা)

## 📌 Memory কি?

**Memory (RAM)** হলো একটি large array of bytes যেখানে প্রতিটি byte এর unique address আছে।

```
┌─────────────────────────────────────────────────────────────┐
│                    Memory Hierarchy                         │
│                                                             │
│            ┌───────────┐                                   │
│            │ Registers │  ← সবচেয়ে দ্রুত, সবচেয়ে ছোট    │
│            └─────┬─────┘                                   │
│                  │                                          │
│            ┌─────▼─────┐                                   │
│            │   Cache   │  ← SRAM, ns                       │
│            └─────┬─────┘                                   │
│                  │                                          │
│         ┌────────▼────────┐                                │
│         │   Main Memory   │  ← DRAM, 100ns                 │
│         │      (RAM)      │                                │
│         └────────┬────────┘                                │
│                  │                                          │
│    ┌─────────────▼─────────────┐                           │
│    │   Secondary Storage       │  ← SSD/HDD, ms            │
│    │   (Disk, SSD)            │                            │
│    └───────────────────────────┘                           │
│                                                             │
│    Speed ↑                         Cost ↓                  │
│    Size ↓                          Persistence ↑           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Memory Manager এর কাজ

```
┌─────────────────────────────────────────────────────────────┐
│  1. Track করা কোন memory ব্যবহৃত, কোনটা free               │
│  2. Process কে memory allocate/deallocate করা              │
│  3. Memory protection দেওয়া                                │
│  4. Swapping করা (RAM ↔ Disk)                              │
│  5. Logical to Physical address translation               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔗 Address Binding

### তিন ধরনের Address

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Symbolic Address                                           │
│  ────────────────                                          │
│  Source code এ variable name                                │
│  int count = 10;  ← 'count' is symbolic                    │
│                                                             │
│  Logical Address (Virtual Address)                          │
│  ─────────────────────────────────                         │
│  CPU generates                                              │
│  Process এর perspective থেকে address                       │
│                                                             │
│  Physical Address                                           │
│  ────────────────                                          │
│  Actual RAM location                                        │
│  Memory unit sees this address                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Address Binding সময়

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  1. Compile Time Binding                                    │
│     • Compiler absolute address generate করে               │
│     • Memory location আগে থেকে জানা থাকতে হয়              │
│     • MS-DOS .COM files                                     │
│                                                             │
│  2. Load Time Binding                                       │
│     • Loader final address determine করে                   │
│     • Relocatable code generate হয়                        │
│     • Load এর সময় address fix হয়                         │
│                                                             │
│  3. Execution Time Binding                                  │
│     • Runtime এ address translate হয়                      │
│     • Process move করা যায়                                │
│     • Hardware support দরকার (MMU)                         │
│     • Modern OS এ ব্যবহৃত                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Logical vs Physical Address

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│      CPU                  MMU                 Memory        │
│    ┌─────┐             ┌──────┐            ┌─────────┐    │
│    │     │  Logical    │      │  Physical  │         │    │
│    │     │────────────►│      │───────────►│         │    │
│    │     │  Address    │      │  Address   │         │    │
│    └─────┘             └──────┘            └─────────┘    │
│                                                             │
│    Logical: 346        Relocation          Physical:       │
│                        Register: 14000     14000 + 346     │
│                                            = 14346         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### MMU (Memory Management Unit)

```c
// Simple relocation scheme
Physical_Address = Logical_Address + Base_Register

// Example:
Base Register = 14000
Logical Address = 346
Physical Address = 14000 + 346 = 14346
```

---

## 🛡️ Memory Protection

### Base and Limit Registers

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│    ┌────────────────────────────────────────────┐          │
│    │                 Memory                      │          │
│    ├────────────────────────────────────────────┤          │
│    │      (Other processes)                      │          │
│    ├────────────────────────────────────────────┤ ◄─ Base  │
│    │                                             │   (300K) │
│    │      Process P                              │          │
│    │                                             │          │
│    ├────────────────────────────────────────────┤ ◄─ Limit │
│    │      (Other processes)                      │   (120K) │
│    ├────────────────────────────────────────────┤          │
│    │                                             │          │
│    └────────────────────────────────────────────┘          │
│                                                             │
│    Base = 300KB                                             │
│    Limit = 120KB                                            │
│    Valid range: 300KB to 420KB                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

```c
// Address validation
if (logical_address >= limit) {
    trap();  // Addressing error
} else {
    physical_address = base + logical_address;
}
```

---

## 🔄 Swapping

```
┌─────────────────────────────────────────────────────────────┐
│                         Swapping                            │
│                                                             │
│     ┌────────┐                        ┌────────────┐       │
│     │  RAM   │                        │    Disk    │       │
│     │        │        Swap Out        │            │       │
│     │  P1    │───────────────────────►│    P1      │       │
│     │        │                        │            │       │
│     │  P2    │◄───────────────────────│    P3      │       │
│     │        │        Swap In         │            │       │
│     └────────┘                        └────────────┘       │
│                                                             │
│    • Process temporarily swap out হয় disk এ               │
│    • আবার memory তে swap in হয়                            │
│    • Context switch এ time লাগে                            │
│                                                             │
│    Swap Time = (Process Size / Transfer Rate) × 2          │
│    (in + out)                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Dynamic Loading ও Linking

### Dynamic Loading

```
সম্পূর্ণ program একবারে load করা হয় না
Routine call হলে তখন load হয়

সুবিধা:
• কম memory ব্যবহার
• Unused routines load হয় না
• No OS support needed (programmer does it)
```

### Dynamic Linking (Shared Libraries)

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Static Linking:          Dynamic Linking:                  │
│                                                             │
│  ┌─────────┐             ┌─────────┐                       │
│  │ Program │             │ Program │                       │
│  ├─────────┤             ├─────────┤                       │
│  │ printf  │             │  stub   │──► ┌─────────┐       │
│  │ code    │             └─────────┘    │ libc.so │       │
│  ├─────────┤                            │ (shared)│       │
│  │ scanf   │                            └─────────┘       │
│  │ code    │                                               │
│  └─────────┘                                               │
│                                                             │
│  বড় executable         ছোট executable                     │
│  Library duplicate হয়   Library share হয়                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## ❓ গুরুত্বপূর্ণ প্রশ্ন

**প্রশ্ন ১:** Logical ও Physical address এর পার্থক্য কি?
**উত্তর:**
- Logical: CPU generates, process এর view
- Physical: Actual RAM location, MMU translates

**প্রশ্ন ২:** Address Binding কখন করা ভালো?
**উত্তর:** Execution time binding সবচেয়ে flexible কারণ process move করা যায়, কিন্তু hardware support দরকার।

**প্রশ্ন ৩:** Base ও Limit register কেন দরকার?
**উত্তর:** Memory protection এর জন্য। এক process অন্য process এর memory access করতে পারবে না।

---
**পরবর্তী:** [Contiguous Allocation](02-contiguous-allocation.md)
