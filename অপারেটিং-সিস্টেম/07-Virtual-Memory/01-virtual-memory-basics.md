# Virtual Memory Basics (ভার্চুয়াল মেমরি মৌলিক)

## 📌 Virtual Memory কি?

**Virtual Memory** হলো এমন একটি technique যা secondary storage (disk) কে RAM এর extension হিসেবে ব্যবহার করে।

```
┌─────────────────────────────────────────────────────────────┐
│                    Virtual Memory Concept                   │
│                                                             │
│   ┌─────────────────────────────┐                          │
│   │     Virtual Address Space    │  ← Process এর view      │
│   │         (4 GB)               │    (বড় address space)  │
│   └─────────────┬───────────────┘                          │
│                 │                                           │
│                 │ Mapping                                   │
│                 ▼                                           │
│   ┌─────────────────────────────┐                          │
│   │     Physical Memory (RAM)    │  ← 1 GB only           │
│   │       (Smaller)              │                          │
│   └─────────────┬───────────────┘                          │
│                 │                                           │
│                 │ Swap                                      │
│                 ▼                                           │
│   ┌─────────────────────────────┐                          │
│   │     Disk (Swap Space)        │  ← Backup storage       │
│   │       (Large)                │                          │
│   └─────────────────────────────┘                          │
│                                                             │
│   Process মনে করে পুরো virtual space তার!                 │
│   কিন্তু শুধু active parts RAM এ থাকে                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 💡 কেন দরকার?

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  1. Run programs larger than physical memory               │
│     Program: 8 GB, RAM: 4 GB → Still runs!                 │
│                                                             │
│  2. Run more programs simultaneously                        │
│     10 programs × 500 MB = 5 GB in 4 GB RAM                │
│                                                             │
│  3. Efficient memory use                                    │
│     শুধু active parts RAM এ                                │
│                                                             │
│  4. Process isolation                                       │
│     প্রতিটি process আলাদা address space                   │
│                                                             │
│  5. Simplified memory management                            │
│     Programmer memory location চিন্তা করে না              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔑 Key Concepts

### Virtual Address Space

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   Process এর Virtual Address Space (32-bit):               │
│                                                             │
│   0xFFFFFFFF ┌────────────────────┐                        │
│              │    Kernel Space    │  ← OS                  │
│   0xC0000000 ├────────────────────┤                        │
│              │       Stack        │  ← Grows down ↓        │
│              │         ↓          │                        │
│              ├─ ─ ─ ─ ─ ─ ─ ─ ─ ─┤                        │
│              │                    │  ← Unused              │
│              ├─ ─ ─ ─ ─ ─ ─ ─ ─ ─┤                        │
│              │         ↑          │                        │
│              │       Heap         │  ← Grows up ↑          │
│              ├────────────────────┤                        │
│              │       BSS          │  ← Uninitialized data  │
│              ├────────────────────┤                        │
│              │       Data         │  ← Initialized data    │
│              ├────────────────────┤                        │
│              │       Text         │  ← Code                │
│   0x00000000 └────────────────────┘                        │
│                                                             │
│   সব process এই একই structure দেখে                        │
│   কিন্তু physical mapping আলাদা!                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Sparse Address Space

```
Process A এর actual usage:
Virtual Space: 4 GB
Actual Used:   100 MB

┌──────────────────────────────────────┐
│████│                                 │ 
│Code│       Unused                    │
│Data│                                 │
│100M│      (3.9 GB unused)            │
└──────────────────────────────────────┘

শুধু used part এর জন্য physical memory allocate
বাকি part এর জন্য page table entry "invalid"
```

---

## 📊 Virtual Memory Components

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  1. Page Table                                              │
│     Virtual page → Physical frame mapping                   │
│     Valid/Invalid bit                                       │
│                                                             │
│  2. MMU (Memory Management Unit)                           │
│     Address translation hardware                            │
│                                                             │
│  3. Swap Space (Page File)                                  │
│     Disk area for swapped pages                             │
│                                                             │
│  4. Page Fault Handler                                      │
│     Handles missing pages                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 How Virtual Memory Works

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   CPU wants to access virtual address 0x12345678           │
│                                                             │
│   Step 1: Check Page Table                                  │
│           ┌─────────────────────────────┐                  │
│           │ Valid? │ Frame/Disk Location│                  │
│           └─────────────────────────────┘                  │
│                                                             │
│   If Valid (Page in RAM):                                   │
│   └──► Translate to physical address → Access memory       │
│                                                             │
│   If Invalid (Page not in RAM):                             │
│   └──► PAGE FAULT!                                         │
│        1. Trap to OS                                        │
│        2. Find page on disk                                 │
│        3. Find free frame (or evict a page)                │
│        4. Load page from disk to frame                      │
│        5. Update page table (valid = 1)                     │
│        6. Restart instruction                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 💾 Swap Space

```
┌─────────────────────────────────────────────────────────────┐
│                    Swap Space on Disk                       │
│                                                             │
│   ┌───────────────────────────────────────────┐            │
│   │                                           │            │
│   │   ┌──────┬──────┬──────┬──────┬─────┐   │            │
│   │   │Page 1│Page 7│Page 3│ Free │ ... │   │            │
│   │   │(P1)  │(P2)  │(P1)  │      │     │   │            │
│   │   └──────┴──────┴──────┴──────┴─────┘   │            │
│   │                                           │            │
│   │   Swap area (swap partition or file)      │            │
│   │                                           │            │
│   └───────────────────────────────────────────┘            │
│                                                             │
│   Linux: /swap partition or swapfile                        │
│   Windows: pagefile.sys                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Benefits of Virtual Memory

| সুবিধা | বিবরণ |
|--------|-------|
| **Larger Programs** | Physical RAM এর চেয়ে বড় program চালানো যায় |
| **More Processes** | বেশি process একসাথে চলতে পারে |
| **Isolation** | প্রতিটি process আলাদা, একে অন্যকে affect করে না |
| **Sharing** | Shared libraries efficiently share করা যায় |
| **Protection** | Invalid access ধরা পড়ে (page fault) |
| **Flexibility** | Memory scattered থাকতে পারে (non-contiguous) |

---

## ⚠️ Virtual Memory এর Cost

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  1. Page Table Space                                        │
│     প্রতিটি process এর জন্য page table লাগে                │
│                                                             │
│  2. Address Translation Overhead                            │
│     প্রতি memory access এ translation                      │
│     (TLB দিয়ে minimize করা হয়)                            │
│                                                             │
│  3. Page Fault Penalty                                      │
│     Disk access খুব slow (ms vs ns)                        │
│     একটি page fault ≈ millions of instructions!            │
│                                                             │
│  4. Thrashing Risk                                          │
│     Too many page faults → system slowdown                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## ❓ গুরুত্বপূর্ণ প্রশ্ন

**প্রশ্ন ১:** Virtual Memory কিভাবে physical memory এর চেয়ে বড় program চালায়?
**উত্তর:** শুধু currently needed pages RAM এ থাকে। বাকি pages disk এ থাকে এবং দরকারে load হয়।

**প্রশ্ন ২:** Virtual Memory ও Swap এর পার্থক্য কি?
**উত্তর:**
- Virtual Memory: Concept/technique
- Swap: Disk এ page store করার জায়গা (virtual memory এর অংশ)

**প্রশ্ন ৩:** সব process same virtual address 0x1000 এ data রাখলে conflict হয় না কেন?
**উত্তর:** প্রতিটি process এর আলাদা page table থাকে। Same virtual address আলাদা physical address এ map হয়।

---
**পরবর্তী:** [Demand Paging](02-demand-paging.md)
