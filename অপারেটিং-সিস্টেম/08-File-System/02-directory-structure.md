# Directory Structure (ডিরেক্টরি স্ট্রাকচার)

## 📌 Directory কি?

**Directory** হলো একটি special file যা অন্যান্য files ও directories এর তথ্য contain করে।

```
┌─────────────────────────────────────────────────────────────┐
│                   Directory Entry                           │
│                                                             │
│   ┌───────────────┬─────────────────────────────┐          │
│   │   File Name   │   Pointer to File Info      │          │
│   ├───────────────┼─────────────────────────────┤          │
│   │   file1.txt   │   → inode 42               │          │
│   │   file2.doc   │   → inode 105              │          │
│   │   subdir/     │   → inode 23               │          │
│   │   image.jpg   │   → inode 87               │          │
│   └───────────────┴─────────────────────────────┘          │
│                                                             │
│   Directory operations:                                     │
│   • Search for file                                         │
│   • Create/delete file                                      │
│   • List files                                              │
│   • Rename file                                             │
│   • Traverse file system                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📂 Directory Structure Types

### ১. Single-Level Directory

```
┌─────────────────────────────────────────────────────────────┐
│              Single-Level Directory                         │
│                                                             │
│                    Root Directory                           │
│                         │                                   │
│     ┌───────┬───────┬───┴───┬───────┬───────┐             │
│     │       │       │       │       │       │             │
│   file1   file2   file3   file4   file5   file6           │
│                                                             │
│   সুবিধা: Simple                                            │
│   অসুবিধা:                                                  │
│   • No grouping                                             │
│   • Name collision (all names must be unique)               │
│   • Difficult to manage many files                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### ২. Two-Level Directory

```
┌─────────────────────────────────────────────────────────────┐
│               Two-Level Directory                           │
│                                                             │
│                   Master Directory                          │
│                         │                                   │
│         ┌───────────────┼───────────────┐                  │
│         │               │               │                  │
│       User1           User2           User3                │
│         │               │               │                  │
│     ┌───┴───┐       ┌───┴───┐       ┌───┴───┐            │
│     │       │       │       │       │       │            │
│   file1   file2   file1   file3   file1   file2          │
│                                                             │
│   সুবিধা:                                                   │
│   • Different users can have same filename                  │
│   • Better isolation                                        │
│                                                             │
│   অসুবিধা:                                                  │
│   • No subdirectories                                       │
│   • No sharing between users                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### ৩. Tree-Structured Directory

```
┌─────────────────────────────────────────────────────────────┐
│              Tree-Structured Directory                      │
│                                                             │
│                          /                                  │
│                          │                                  │
│         ┌────────────────┼────────────────┐                │
│         │                │                │                │
│       home              etc              var               │
│         │                │                │                │
│     ┌───┴───┐        ┌───┴───┐       ┌───┴───┐           │
│     │       │        │       │       │       │           │
│   user1   user2    passwd  hosts    log   spool          │
│     │                                 │                    │
│   ┌─┴─┐                           ┌───┴───┐              │
│   │   │                           │       │              │
│ docs code                      syslog  auth.log          │
│                                                             │
│   সুবিধা:                                                   │
│   • Efficient organization                                  │
│   • Hierarchical structure                                  │
│   • Absolute and relative paths                             │
│                                                             │
│   Modern systems use this!                                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### ৪. Acyclic-Graph Directory

```
┌─────────────────────────────────────────────────────────────┐
│             Acyclic-Graph Directory                         │
│                                                             │
│   Allows SHARING of files/directories                       │
│                                                             │
│                    /                                        │
│                    │                                        │
│         ┌──────────┴──────────┐                            │
│         │                     │                            │
│       user1                 user2                          │
│         │                     │                            │
│     ┌───┴───┐                 │                            │
│     │       │                 │                            │
│   docs    shared ◄────────────┘  (shared folder)           │
│             │                                               │
│         ┌───┴───┐                                          │
│         │       │                                          │
│      file1.txt file2.txt                                   │
│                                                             │
│   Implementation:                                           │
│   • Hard links (same inode)                                 │
│   • Symbolic links (pointer file)                           │
│                                                             │
│   সমস্যা:                                                   │
│   • Deletion: When to actually delete?                      │
│   • Reference counting needed                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔗 Links

### Hard Link

```
$ ln file1.txt hardlink.txt

┌─────────────┐     ┌─────────────┐
│ file1.txt   │     │hardlink.txt │
└──────┬──────┘     └──────┬──────┘
       │                   │
       └─────────┬─────────┘
                 ▼
          ┌───────────┐
          │ inode 42  │
          │           │
          │ link_count│ = 2
          │ data_ptr  │ ──► actual data blocks
          └───────────┘

• Same inode
• Both names are "real"
• File deleted when link_count = 0
• Cannot cross filesystems
• Cannot link directories (usually)
```

### Symbolic (Soft) Link

```
$ ln -s /path/to/file1.txt symlink.txt

┌─────────────┐     ┌─────────────────────────┐
│ file1.txt   │     │ symlink.txt             │
└──────┬──────┘     │ (type: symbolic link)   │
       │            │ content: "/path/to/file1"│
       ▼            └──────────┬──────────────┘
  ┌─────────┐                  │
  │inode 42 │◄─────────────────┘ (follows path)
  └─────────┘

• Separate file (different inode)
• Contains path to target
• Can cross filesystems
• Can link to directories
• Dangling link if target deleted
```

---

## 📍 Path Names

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Absolute Path:                                             │
│  Starts from root (/)                                       │
│  /home/user/documents/file.txt                              │
│                                                             │
│  Relative Path:                                             │
│  Starts from current directory                              │
│  ./documents/file.txt                                       │
│  ../other_user/file.txt                                     │
│                                                             │
│  Special Entries:                                           │
│  .   = current directory                                    │
│  ..  = parent directory                                     │
│  ~   = home directory (shell expansion)                     │
│                                                             │
│  Current Directory: /home/user                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Relative           → Absolute                       │   │
│  │ ./file.txt         → /home/user/file.txt            │   │
│  │ ../file.txt        → /home/file.txt                 │   │
│  │ docs/report.pdf    → /home/user/docs/report.pdf     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 💻 Directory Operations

```c
// C/POSIX directory operations

// Open directory
DIR *dir = opendir("/home/user");

// Read entries
struct dirent *entry;
while ((entry = readdir(dir)) != NULL) {
    printf("%s\n", entry->d_name);
}

// Close directory
closedir(dir);

// Create directory
mkdir("/home/user/newdir", 0755);

// Remove directory (must be empty)
rmdir("/home/user/newdir");

// Change current directory
chdir("/home/user/docs");

// Get current directory
char cwd[PATH_MAX];
getcwd(cwd, sizeof(cwd));
```

---

## 📊 Directory Implementation

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Linear List:                                               │
│  ┌─────────┬─────────┬─────────┬─────────┐                │
│  │ entry 1 │ entry 2 │ entry 3 │   ...   │                │
│  └─────────┴─────────┴─────────┴─────────┘                │
│  Simple, slow search O(n)                                   │
│                                                             │
│  Hash Table:                                                │
│  ┌─────┐                                                   │
│  │  0  │ → entry_a                                         │
│  │  1  │ → NULL                                            │
│  │  2  │ → entry_b → entry_c (collision chain)            │
│  │ ... │                                                   │
│  └─────┘                                                   │
│  Fast search O(1), collision handling needed                │
│                                                             │
│  B-tree:                                                    │
│  Used by modern filesystems (NTFS, ext4)                    │
│  Fast search, insert, delete O(log n)                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## ❓ গুরুত্বপূর্ণ প্রশ্ন

**প্রশ্ন ১:** Hard link ও Symbolic link এর পার্থক্য কি?

| Hard Link | Symbolic Link |
|-----------|---------------|
| Same inode | Different inode |
| Can't cross filesystem | Can cross filesystem |
| Can't link directories | Can link directories |
| Target delete করলেও works | Dangling link হয় |

**প্রশ্ন ২:** Acyclic-graph directory তে cycle allow করা হয় না কেন?
**উত্তর:** Cycle থাকলে infinite loop হতে পারে (traversal, deletion, search এ)। Directory এর infinite nesting হয়ে যায়।

**প্রশ্ন ৩:** . এবং .. entry কিসের জন্য?
**উত্তর:**
- `.` = Current directory (self reference)
- `..` = Parent directory (upward navigation)

---
**পূর্ববর্তী:** [File Concepts](01-file-concepts.md)  
**পরবর্তী:** [File Allocation Methods](03-file-allocation.md)
