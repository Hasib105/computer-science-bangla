# Inter-Process Communication (IPC)

## 📌 IPC কি?

**Inter-Process Communication (IPC)** হলো প্রসেসগুলোর মধ্যে ডেটা আদান-প্রদান ও সমন্বয়ের পদ্ধতি।

```
┌─────────────────┐         ┌─────────────────┐
│    Process A    │  ←────→ │    Process B    │
│                 │   IPC   │                 │
│   Producer      │         │   Consumer      │
└─────────────────┘         └─────────────────┘
```

---

## 🔄 IPC এর প্রয়োজনীয়তা

### কেন প্রসেসগুলোকে যোগাযোগ করতে হয়?

1. **Information Sharing:** একই ডেটা একাধিক প্রসেস ব্যবহার করা
2. **Computation Speedup:** কাজ ভাগ করে দ্রুত সম্পন্ন করা
3. **Modularity:** বড় সিস্টেমকে ছোট ছোট প্রসেসে ভাগ করা
4. **Convenience:** ইউজার একাধিক কাজ একসাথে করতে পারে

---

## 📊 IPC এর প্রকারভেদ

```
                        IPC Methods
                            │
          ┌─────────────────┼─────────────────┐
          │                 │                 │
    Shared Memory      Message Passing    Signals
          │                 │
    ┌─────┴─────┐     ┌─────┴─────┐
    │           │     │           │
  Direct    Indirect  Pipes    Sockets
```

---

## 1️⃣ Shared Memory

### ধারণা
একই মেমরি এলাকা একাধিক প্রসেস অ্যাক্সেস করে।

```
┌─────────────────────────────────────────────────────┐
│                    Shared Memory                     │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │                                              │   │
│  │            Shared Memory Region              │   │
│  │                                              │   │
│  └───────────────────┬──────────────────────────┘   │
│                      │                              │
├──────────────────────┼──────────────────────────────┤
│          ↓           │           ↓                  │
│  ┌─────────────┐     │     ┌─────────────┐          │
│  │  Process A  │     │     │  Process B  │          │
│  │  ┌───────┐  │     │     │  ┌───────┐  │          │
│  │  │ Write │  │─────┼────→│  │ Read  │  │          │
│  │  └───────┘  │     │     │  └───────┘  │          │
│  └─────────────┘     │     └─────────────┘          │
└──────────────────────┴──────────────────────────────┘
```

### POSIX Shared Memory উদাহরণ

**Writer Process:**
```c
#include <sys/mman.h>
#include <fcntl.h>
#include <string.h>

int main() {
    // Shared memory তৈরি
    int fd = shm_open("/my_shm", O_CREAT | O_RDWR, 0666);
    ftruncate(fd, 1024);
    
    // Memory map করা
    char* ptr = mmap(NULL, 1024, PROT_WRITE, MAP_SHARED, fd, 0);
    
    // ডেটা লেখা
    strcpy(ptr, "Hello from Writer!");
    
    return 0;
}
```

**Reader Process:**
```c
#include <sys/mman.h>
#include <fcntl.h>
#include <stdio.h>

int main() {
    // Shared memory খোলা
    int fd = shm_open("/my_shm", O_RDONLY, 0666);
    
    // Memory map করা
    char* ptr = mmap(NULL, 1024, PROT_READ, MAP_SHARED, fd, 0);
    
    // ডেটা পড়া
    printf("Message: %s\n", ptr);
    
    return 0;
}
```

### সুবিধা ও অসুবিধা

| সুবিধা | অসুবিধা |
|--------|---------|
| দ্রুত | Synchronization নিজে করতে হয় |
| বড় ডেটার জন্য efficient | Race condition সম্ভাবনা |
| Kernel involvement কম | Complex programming |

---

## 2️⃣ Message Passing

### ধারণা
প্রসেসগুলো মেসেজ পাঠায় ও গ্রহণ করে। Kernel মধ্যস্থতা করে।

```
┌─────────────┐                      ┌─────────────┐
│  Process A  │                      │  Process B  │
│             │      Message         │             │
│   send(B,   │─────────────────────→│   receive() │
│    msg)     │                      │             │
└─────────────┘                      └─────────────┘
              ↘                    ↗
               ↘                  ↗
                ┌────────────────┐
                │     Kernel     │
                │   Message      │
                │    Queue       │
                └────────────────┘
```

### Direct vs Indirect Communication

#### Direct Communication
```
send(P, message)    - P প্রসেসে পাঠাও
receive(Q, message) - Q প্রসেস থেকে নাও

সমস্যা: প্রসেস ID জানতে হয়
```

#### Indirect Communication (Mailbox)
```
┌─────────────┐     ┌──────────┐     ┌─────────────┐
│  Process A  │────→│ Mailbox  │←────│  Process B  │
│  send(M,msg)│     │    M     │     │receive(M)   │
└─────────────┘     └──────────┘     └─────────────┘

সুবিধা: প্রসেস ID জানার দরকার নেই
```

### Synchronous vs Asynchronous

```
Synchronous (Blocking):
┌─────────────┐                    ┌─────────────┐
│  Process A  │                    │  Process B  │
│             │                    │             │
│   send()    │────────────────────│             │
│   blocked   │                    │             │
│      ↓      │                    │  receive()  │
│   unblock   │←───────────────────│             │
└─────────────┘                    └─────────────┘

Asynchronous (Non-blocking):
┌─────────────┐                    ┌─────────────┐
│  Process A  │                    │  Process B  │
│             │                    │             │
│   send()    │────────────────────│             │
│  continues  │                    │             │
│     ...     │                    │  receive()  │
│     ...     │                    │  (later)    │
└─────────────┘                    └─────────────┘
```

---

## 3️⃣ Pipes

### Anonymous Pipe
Parent-child প্রসেসের মধ্যে unidirectional communication।

```
┌────────────┐      ┌──────────────────┐      ┌────────────┐
│   Parent   │      │       Pipe       │      │   Child    │
│            │      │                  │      │            │
│  write()  ────────→  ════════════  ────────→  read()    │
│            │      │   fd[0]  fd[1]   │      │            │
└────────────┘      └──────────────────┘      └────────────┘

fd[0] = read end
fd[1] = write end
```

**উদাহরণ:**
```c
#include <stdio.h>
#include <unistd.h>
#include <string.h>

int main() {
    int fd[2];
    pipe(fd);
    
    pid_t pid = fork();
    
    if (pid == 0) {
        // Child: read from pipe
        close(fd[1]);  // close write end
        char buffer[100];
        read(fd[0], buffer, sizeof(buffer));
        printf("Child received: %s\n", buffer);
        close(fd[0]);
    } else {
        // Parent: write to pipe
        close(fd[0]);  // close read end
        char* msg = "Hello from parent!";
        write(fd[1], msg, strlen(msg) + 1);
        close(fd[1]);
    }
    
    return 0;
}
```

### Named Pipe (FIFO)
যেকোনো প্রসেসের মধ্যে communication।

```bash
# FIFO তৈরি
mkfifo /tmp/myfifo

# Terminal 1 (Writer)
echo "Hello" > /tmp/myfifo

# Terminal 2 (Reader)
cat /tmp/myfifo
```

```c
// Writer
int fd = open("/tmp/myfifo", O_WRONLY);
write(fd, "Hello", 6);

// Reader
int fd = open("/tmp/myfifo", O_RDONLY);
read(fd, buffer, 100);
```

---

## 4️⃣ Message Queues

### ধারণা
Kernel-managed message queue যেখানে structured messages থাকে।

```
┌─────────────────────────────────────────────────────┐
│                   Message Queue                      │
│                                                      │
│    ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐          │
│    │ Msg1 │─→│ Msg2 │─→│ Msg3 │─→│ Msg4 │          │
│    │Type:1│  │Type:2│  │Type:1│  │Type:3│          │
│    └──────┘  └──────┘  └──────┘  └──────┘          │
│                                                      │
│    ↑ msgsnd()                      msgrcv() ↓       │
│    │                                        │       │
│  ┌─┴──────────┐                  ┌──────────┴─┐     │
│  │  Process A │                  │  Process B │     │
│  └────────────┘                  └────────────┘     │
└─────────────────────────────────────────────────────┘
```

### সুবিধা
- Message type অনুযায়ী select করা যায়
- Multiple writers, multiple readers
- Persistence (প্রসেস শেষ হলেও queue থাকে)

---

## 5️⃣ Sockets

### ধারণা
Network বা local machine এ প্রসেস যোগাযোগ।

```
┌───────────────────────────────────────────────────────────┐
│                    Socket Communication                    │
│                                                            │
│   Machine A                            Machine B           │
│  ┌──────────────┐                    ┌──────────────┐     │
│  │   Process    │                    │   Process    │     │
│  │   (Client)   │                    │   (Server)   │     │
│  │              │                    │              │     │
│  │   socket()   │                    │   socket()   │     │
│  │   connect()  │                    │   bind()     │     │
│  │              │                    │   listen()   │     │
│  │              │                    │   accept()   │     │
│  │   send()     │────── Network ─────│   recv()     │     │
│  │   recv()     │←───────────────────│   send()     │     │
│  │   close()    │                    │   close()    │     │
│  └──────────────┘                    └──────────────┘     │
└───────────────────────────────────────────────────────────┘
```

### Socket Types
- **SOCK_STREAM:** TCP (reliable, connection-oriented)
- **SOCK_DGRAM:** UDP (unreliable, connectionless)

---

## 6️⃣ Signals

### ধারণা
Asynchronous notification to a process।

```
┌─────────────┐     Signal (SIGTERM)    ┌─────────────┐
│  Process A  │ ───────────────────────→│  Process B  │
│   kill()    │                         │  Handler()  │
└─────────────┘                         └─────────────┘
```

### Common Signals

| Signal | Number | Description |
|--------|--------|-------------|
| SIGINT | 2 | Ctrl+C |
| SIGKILL | 9 | Force kill (cannot catch) |
| SIGTERM | 15 | Graceful termination |
| SIGSEGV | 11 | Segmentation fault |
| SIGCHLD | 17 | Child terminated |

### Signal Handler উদাহরণ

```c
#include <signal.h>
#include <stdio.h>

void handler(int sig) {
    printf("Caught signal %d\n", sig);
}

int main() {
    signal(SIGINT, handler);  // Ctrl+C handle করা
    
    while(1) {
        printf("Running...\n");
        sleep(1);
    }
    
    return 0;
}
```

---

## 📊 IPC Methods তুলনা

| Method | Speed | Complexity | Use Case |
|--------|-------|------------|----------|
| Shared Memory | Fast | Complex | Large data |
| Pipes | Medium | Simple | Parent-child |
| Message Queue | Medium | Medium | Structured data |
| Sockets | Slow | Complex | Network/distributed |
| Signals | Fast | Simple | Simple notifications |

---

## ❓ গুরুত্বপূর্ণ প্রশ্ন

**প্রশ্ন ১:** Shared Memory ও Message Passing এর পার্থক্য কি?

| Shared Memory | Message Passing |
|---------------|-----------------|
| দ্রুত | তুলনামূলক ধীর |
| Synchronization নিজে করতে হয় | Kernel করে দেয় |
| Kernel call কম | প্রতি message এ kernel call |

**প্রশ্ন ২:** Pipe এবং Named Pipe এর পার্থক্য কি?

| Pipe | Named Pipe (FIFO) |
|------|-------------------|
| শুধু related processes | যেকোনো process |
| Memory তে থাকে | File system এ থাকে |
| fork() দরকার | নাম দিয়ে access |

---
**পূর্ববর্তী:** [থ্রেড](05-threads.md)  
**পরবর্তী ফোল্ডার:** [CPU Scheduling](../03-CPU-Scheduling/README.md)
