# প্রসেস ধারণা (Process Concept)

## 📌 প্রসেস কি?

**প্রসেস** হলো একটি চলমান প্রোগ্রাম। যখন কোনো প্রোগ্রাম মেমরিতে লোড হয়ে এক্সিকিউট হতে শুরু করে, তখন তাকে প্রসেস বলে।

### প্রোগ্রাম vs প্রসেস

```
┌─────────────────────┐        ┌─────────────────────┐
│      প্রোগ্রাম       │        │       প্রসেস        │
├─────────────────────┤        ├─────────────────────┤
│ • নিষ্ক্রিয় সত্তা   │   →    │ • সক্রিয় সত্তা      │
│ • ডিস্কে থাকে       │  লোড   │ • মেমরিতে থাকে      │
│ • Static           │        │ • Dynamic          │
│ • শুধু কোড          │        │ • কোড + ডেটা +      │
│                     │        │   স্ট্যাক + হিপ      │
└─────────────────────┘        └─────────────────────┘
```

### উদাহরণ
```
chrome.exe (ডিস্কে) = প্রোগ্রাম
Chrome চলছে (মেমরিতে) = প্রসেস

একই প্রোগ্রাম থেকে একাধিক প্রসেস হতে পারে:
chrome.exe → Process 1 (Tab 1)
          → Process 2 (Tab 2)
          → Process 3 (Tab 3)
```

---

## 🏗️ প্রসেসের মেমরি লেআউট

```
High Address
┌─────────────────────────┐
│         Stack           │ ← Local variables, function calls
│           ↓             │   (উপর থেকে নিচে বাড়ে)
├─────────────────────────┤
│                         │
│      Free Space         │
│                         │
├─────────────────────────┤
│           ↑             │
│          Heap           │ ← Dynamic memory (malloc, new)
│                         │   (নিচ থেকে উপরে বাড়ে)
├─────────────────────────┤
│     Data Section        │ ← Global & static variables
│  ┌───────────────────┐  │
│  │ Initialized Data  │  │ ← int x = 5;
│  ├───────────────────┤  │
│  │Uninitialized (BSS)│  │ ← int y;
│  └───────────────────┘  │
├─────────────────────────┤
│      Text Section       │ ← Program code (read-only)
│      (Code)             │
└─────────────────────────┘
Low Address
```

### প্রতিটি সেকশনের বিবরণ

| সেকশন | বিবরণ | উদাহরণ |
|-------|-------|--------|
| **Text** | প্রোগ্রামের কোড | ফাংশন, ইনস্ট্রাকশন |
| **Data** | ইনিশিয়ালাইজড গ্লোবাল ভেরিয়েবল | `int x = 10;` |
| **BSS** | আনইনিশিয়ালাইজড গ্লোবাল ভেরিয়েবল | `int y;` |
| **Heap** | ডায়নামিক মেমরি | `malloc()`, `new` |
| **Stack** | ফাংশন কল, লোকাল ভেরিয়েবল | `int z = 5;` (ফাংশনে) |

---

## 📋 প্রসেস অ্যাট্রিবিউটস

প্রতিটি প্রসেসের কিছু বৈশিষ্ট্য থাকে:

```
┌────────────────────────────────────────┐
│            Process Attributes          │
├────────────────────────────────────────┤
│ Process ID (PID)      : 1234           │
│ Parent Process ID     : 1230           │
│ Process State         : Running        │
│ Priority              : 5              │
│ Program Counter       : 0x00401000     │
│ CPU Registers         : [...]          │
│ Memory Pointers       : [...]          │
│ I/O Information       : [...]          │
│ Accounting Info       : CPU time used  │
└────────────────────────────────────────┘
```

### গুরুত্বপূর্ণ অ্যাট্রিবিউটস

1. **PID (Process ID):** প্রতিটি প্রসেসের ইউনিক আইডেন্টিফায়ার
2. **State:** প্রসেসের বর্তমান অবস্থা
3. **Priority:** শিডিউলিং এ গুরুত্ব
4. **Program Counter:** পরবর্তী ইনস্ট্রাকশনের অ্যাড্রেস
5. **Memory Info:** প্রসেসের মেমরি বাউন্ডারি

---

## 🔄 প্রসেস স্টেট ডায়াগ্রাম (সংক্ষিপ্ত)

```
                 ┌────────────┐
      New ──────→│   Ready    │←─────────────┐
                 └─────┬──────┘              │
                       │                     │
                       │ Scheduler           │ I/O Complete
                       │ Dispatch            │
                       ↓                     │
                 ┌────────────┐        ┌────────────┐
                 │  Running   │───────→│  Waiting   │
                 └─────┬──────┘  I/O   └────────────┘
                       │
                       │ Exit
                       ↓
                 ┌────────────┐
                 │ Terminated │
                 └────────────┘
```

---

## 🧮 প্রসেস vs থ্রেড (প্রাথমিক তুলনা)

```
┌─────────────────┐      ┌─────────────────┐
│    Process 1    │      │    Process 2    │
│ ┌─────────────┐ │      │ ┌─────────────┐ │
│ │   Thread 1  │ │      │ │   Thread 1  │ │
│ │   Thread 2  │ │      │ │   Thread 2  │ │
│ │   Thread 3  │ │      │ └─────────────┘ │
│ └─────────────┘ │      │                 │
│ Own Memory      │      │ Own Memory      │
│ Own Resources   │      │ Own Resources   │
└─────────────────┘      └─────────────────┘
     আলাদা                    আলাদা
```

| বৈশিষ্ট্য | প্রসেস | থ্রেড |
|----------|--------|-------|
| মেমরি | নিজস্ব | শেয়ার্ড |
| তৈরির সময় | বেশি | কম |
| যোগাযোগ | জটিল (IPC) | সহজ |
| Context Switch | ধীর | দ্রুত |
| Overhead | বেশি | কম |

---

## 💻 বাস্তব উদাহরণ

### C প্রোগ্রামে মেমরি সেকশন
```c
#include <stdio.h>
#include <stdlib.h>

int global_init = 10;      // Data Section
int global_uninit;         // BSS Section

void function() {
    int local = 5;         // Stack
    int *ptr = malloc(10); // Heap
}

int main() {               // Text Section
    function();
    return 0;
}
```

### Linux এ প্রসেস দেখা
```bash
# সব প্রসেস দেখা
ps aux

# নির্দিষ্ট প্রসেসের তথ্য
ps -p 1234

# প্রসেস ট্রি
pstree

# আউটপুট উদাহরণ:
# PID  PPID  CMD
# 1    0     init
# 100  1     bash
# 150  100   vim
```

---

## ❓ গুরুত্বপূর্ণ প্রশ্ন

**প্রশ্ন ১:** প্রোগ্রাম ও প্রসেসের মধ্যে পার্থক্য কি?

**উত্তর:**
| প্রোগ্রাম | প্রসেস |
|----------|--------|
| Passive (নিষ্ক্রিয়) | Active (সক্রিয়) |
| ডিস্কে সংরক্ষিত | মেমরিতে চলমান |
| শুধু কোড | কোড + ডেটা + স্টেট |

**প্রশ্ন ২:** Stack ও Heap এর পার্থক্য কি?

**উত্তর:**
| Stack | Heap |
|-------|------|
| Automatic allocation | Manual allocation |
| LIFO structure | Random access |
| Fixed size | Dynamic size |
| দ্রুত | ধীর |
| Local variables | Dynamic data |

**প্রশ্ন ৩:** একটি প্রোগ্রাম থেকে কতটি প্রসেস হতে পারে?
**উত্তর:** একাধিক। যেমন Chrome ব্রাউজারে প্রতিটি ট্যাব আলাদা প্রসেস।

---
**পরবর্তী:** [প্রসেস স্টেট](02-process-states.md)
