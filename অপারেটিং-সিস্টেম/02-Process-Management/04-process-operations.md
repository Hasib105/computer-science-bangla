# প্রসেস অপারেশন (Process Operations)

## 📌 মূল অপারেশনগুলো

প্রসেসের উপর OS চারটি প্রধান অপারেশন করে:

```
┌────────────────────────────────────────┐
│         Process Operations             │
├────────────────────────────────────────┤
│  1. Create   - প্রসেস তৈরি             │
│  2. Execute  - প্রসেস চালানো           │
│  3. Suspend  - প্রসেস স্থগিত করা       │
│  4. Terminate- প্রসেস বন্ধ করা         │
└────────────────────────────────────────┘
```

---

## 1️⃣ প্রসেস তৈরি (Process Creation)

### প্রসেস তৈরির কারণ

| কারণ | বিবরণ |
|------|-------|
| System Initialization | বুট আপের সময় |
| User Request | ইউজার প্রোগ্রাম রান করলে |
| Batch Job | ব্যাচ সিস্টেমে |
| System Call | fork(), exec() |

### Parent-Child সম্পর্ক

```
                    init (PID 1)
                         │
            ┌────────────┼────────────┐
            │            │            │
         systemd       login       sshd
            │            │            │
     ┌──────┼──────┐    bash      ┌──┴──┐
     │      │      │     │        │     │
  apache  mysql  redis  vim      ssh   sftp
     │
  ┌──┼──┐
  │  │  │
worker threads
```

**নিয়ম:**
- প্রতিটি প্রসেসের একজন Parent থাকে
- একটি প্রসেস একাধিক Child তৈরি করতে পারে
- PID 1 (init/systemd) হলো সব প্রসেসের আদি Parent

### fork() System Call

```c
#include <stdio.h>
#include <unistd.h>

int main() {
    pid_t pid = fork();  // নতুন প্রসেস তৈরি
    
    if (pid < 0) {
        // Error
        printf("Fork failed!\n");
    }
    else if (pid == 0) {
        // Child Process
        printf("I am child, PID: %d\n", getpid());
    }
    else {
        // Parent Process
        printf("I am parent, child PID: %d\n", pid);
    }
    
    return 0;
}
```

**Output:**
```
I am parent, child PID: 1234
I am child, PID: 1234
```

### fork() কিভাবে কাজ করে

```
Before fork():
┌─────────────────────┐
│   Parent Process    │
│   PID: 1000         │
│   ┌───────────────┐ │
│   │ Code          │ │
│   │ Data          │ │
│   │ Stack         │ │
│   └───────────────┘ │
└─────────────────────┘

After fork():
┌─────────────────────┐     ┌─────────────────────┐
│   Parent Process    │     │   Child Process     │
│   PID: 1000         │     │   PID: 1001         │
│   fork() returns:   │     │   fork() returns:   │
│   1001 (child PID)  │     │   0                 │
│   ┌───────────────┐ │     │   ┌───────────────┐ │
│   │ Code   (copy) │ │     │   │ Code   (copy) │ │
│   │ Data   (copy) │ │     │   │ Data   (copy) │ │
│   │ Stack  (copy) │ │     │   │ Stack  (copy) │ │
│   └───────────────┘ │     │   └───────────────┘ │
└─────────────────────┘     └─────────────────────┘
```

### exec() System Call

নতুন প্রোগ্রাম লোড করে বর্তমান প্রসেসে।

```c
#include <unistd.h>

int main() {
    pid_t pid = fork();
    
    if (pid == 0) {
        // Child: নতুন প্রোগ্রাম রান করো
        execl("/bin/ls", "ls", "-l", NULL);
        // exec সফল হলে এই লাইন execute হবে না
    }
    
    // Parent continues here
    return 0;
}
```

```
fork() + exec() = নতুন প্রোগ্রাম চালানো

┌──────────────┐     fork()      ┌──────────────┐
│    Parent    │ ───────────────→│    Child     │
│   (shell)    │                 │  (shell copy)│
└──────────────┘                 └──────┬───────┘
                                        │
                                  exec("ls")
                                        │
                                        ↓
                                 ┌──────────────┐
                                 │    Child     │
                                 │    (ls)      │
                                 └──────────────┘
```

---

## 2️⃣ প্রসেস সমাপ্তি (Process Termination)

### সমাপ্তির কারণ

| কারণ | বিবরণ |
|------|-------|
| Normal Exit | প্রোগ্রাম সম্পূর্ণ হলে |
| Error Exit | এরর রিটার্ন করলে |
| Fatal Error | Illegal instruction, segfault |
| Killed | অন্য প্রসেস kill করলে |

### exit() System Call

```c
#include <stdlib.h>

int main() {
    // কিছু কাজ...
    
    if (error) {
        exit(1);  // Error exit
    }
    
    exit(0);  // Normal exit
}
```

### kill() System Call

```c
#include <signal.h>

// PID 1234 কে SIGTERM পাঠাও
kill(1234, SIGTERM);

// PID 1234 কে SIGKILL পাঠাও (force kill)
kill(1234, SIGKILL);
```

### wait() System Call

Parent, Child এর জন্য অপেক্ষা করে।

```c
#include <sys/wait.h>

int main() {
    pid_t pid = fork();
    
    if (pid == 0) {
        // Child
        printf("Child running\n");
        sleep(2);
        exit(0);
    }
    else {
        // Parent
        int status;
        wait(&status);  // Child শেষ হওয়া পর্যন্ত অপেক্ষা
        printf("Child finished\n");
    }
    
    return 0;
}
```

### Orphan ও Zombie Process

#### Orphan Process
```
Parent শেষ হয়ে গেছে, কিন্তু Child এখনও চলছে

Before:                        After Parent exits:
┌──────────┐                   ┌──────────┐
│  Parent  │                   │   init   │ (new parent)
│  PID:100 │                   │  PID:1   │
└────┬─────┘                   └────┬─────┘
     │                              │
┌────┴─────┐                   ┌────┴─────┐
│  Child   │    ───────→       │  Child   │ (orphan)
│  PID:101 │                   │  PID:101 │
└──────────┘                   └──────────┘

init প্রসেস orphan দের adopt করে
```

#### Zombie Process
```
Child শেষ হয়ে গেছে, কিন্তু Parent wait() করেনি

┌──────────┐
│  Parent  │ (wait করেনি)
│  PID:100 │
└────┬─────┘
     │
┌────┴─────┐
│  Zombie  │ (PCB থেকে গেছে)
│  PID:101 │
│ STATE: Z │
└──────────┘

সমস্যা: PCB memory নষ্ট হচ্ছে
সমাধান: Parent এ wait() call করা
```

---

## 3️⃣ প্রসেস হায়ারার্কি

### Linux এ Process Tree

```bash
$ pstree

systemd─┬─ModemManager───2*[{ModemManager}]
        ├─NetworkManager───2*[{NetworkManager}]
        ├─accounts-daemon───2*[{accounts-daemon}]
        ├─gnome-shell───3*[{gnome-shell}]
        ├─gnome-terminal─┬─bash───vim
        │                └─bash───pstree
        └─sshd───sshd───bash───python
```

### Windows এ Process Tree

```
System
├── smss.exe
├── csrss.exe
├── wininit.exe
│   └── services.exe
│       ├── svchost.exe
│       ├── spoolsv.exe
│       └── ...
└── winlogon.exe
    └── explorer.exe
        ├── chrome.exe
        └── notepad.exe
```

---

## 📊 Process Operations Summary

```
┌─────────────────────────────────────────────────────────────┐
│                    Process Lifecycle                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│    fork()                                                   │
│      │                                                      │
│      ↓                                                      │
│  ┌───────┐    exec()    ┌──────────┐                       │
│  │  New  │ ───────────→ │ Different│                       │
│  │Process│              │ Program  │                       │
│  └───┬───┘              └────┬─────┘                       │
│      │                       │                              │
│      ↓                       ↓                              │
│  ┌───────────────────────────────┐                         │
│  │        Runs/Executes          │                         │
│  └───────────────┬───────────────┘                         │
│                  │                                          │
│      ┌───────────┼───────────┐                             │
│      │           │           │                              │
│   exit()      signal()    abort()                          │
│      │           │           │                              │
│      ↓           ↓           ↓                              │
│  ┌───────────────────────────────┐                         │
│  │        Terminated             │                         │
│  └───────────────────────────────┘                         │
│                  │                                          │
│              wait()                                         │
│                  │                                          │
│                  ↓                                          │
│  ┌───────────────────────────────┐                         │
│  │      Resources Released       │                         │
│  └───────────────────────────────┘                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 💻 সম্পূর্ণ উদাহরণ

```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>

int main() {
    printf("Parent PID: %d\n", getpid());
    
    pid_t pid = fork();
    
    if (pid < 0) {
        perror("Fork failed");
        exit(1);
    }
    else if (pid == 0) {
        // Child Process
        printf("Child PID: %d, Parent: %d\n", getpid(), getppid());
        
        // নতুন প্রোগ্রাম চালাও
        char *args[] = {"ls", "-la", NULL};
        execvp("ls", args);
        
        // exec ফেইল করলে
        perror("Exec failed");
        exit(1);
    }
    else {
        // Parent Process
        printf("Parent: Waiting for child %d\n", pid);
        
        int status;
        waitpid(pid, &status, 0);
        
        if (WIFEXITED(status)) {
            printf("Child exited with status: %d\n", WEXITSTATUS(status));
        }
    }
    
    return 0;
}
```

---

## ❓ গুরুত্বপূর্ণ প্রশ্ন

**প্রশ্ন ১:** fork() কি রিটার্ন করে?

**উত্তর:**
- Parent এ: Child এর PID
- Child এ: 0
- Error হলে: -1

**প্রশ্ন ২:** Zombie ও Orphan এর পার্থক্য কি?

| Zombie | Orphan |
|--------|--------|
| Child শেষ, Parent wait করেনি | Parent শেষ, Child চলছে |
| PCB থেকে যায় | init দ্বারা adopt হয় |
| মেমরি সমস্যা করে | সমস্যা করে না |

**প্রশ্ন ৩:** exec() কল এর পর কি হয়?
**উত্তর:** বর্তমান প্রসেসের কোড নতুন প্রোগ্রাম দিয়ে replace হয়। exec() এর পরের কোড কখনো execute হয় না (সফল হলে)।

---
**পূর্ববর্তী:** [PCB](03-pcb.md)  
**পরবর্তী:** [থ্রেড](05-threads.md)
