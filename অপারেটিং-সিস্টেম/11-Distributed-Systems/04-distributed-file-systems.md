# Distributed File Systems

## 📌 Distributed File System কি?

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   DFS = File system spread across multiple machines         │
│         Access remote files as if local                     │
│                                                             │
│   ┌────────────────────────────────────────────────────┐   │
│   │                     Clients                        │   │
│   │  ┌────────┐   ┌────────┐   ┌────────┐             │   │
│   │  │Client 1│   │Client 2│   │Client 3│             │   │
│   │  └───┬────┘   └───┬────┘   └───┬────┘             │   │
│   │      │            │            │                   │   │
│   │      └────────────┼────────────┘                   │   │
│   │                   │                                │   │
│   │           ┌───────┴───────┐                       │   │
│   │           │    Network    │                       │   │
│   │           └───────┬───────┘                       │   │
│   │                   │                                │   │
│   │      ┌────────────┼────────────┐                  │   │
│   │      │            │            │                   │   │
│   │  ┌───┴────┐   ┌───┴────┐   ┌───┴────┐            │   │
│   │  │Server 1│   │Server 2│   │Server 3│            │   │
│   │  │ /data  │   │ /apps  │   │ /home  │            │   │
│   │  └────────┘   └────────┘   └────────┘            │   │
│   └────────────────────────────────────────────────────┘   │
│                                                             │
│   User accesses: /home/user/file.txt                       │
│   Actually on: Server 3                                     │
│   User doesn't need to know!                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 DFS Requirements

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   1. Transparency                                           │
│      • Access: Same API as local files                     │
│      • Location: Path doesn't reveal server                 │
│      • Migration: Files can move                            │
│      • Replication: Multiple copies transparent            │
│                                                             │
│   2. Performance                                            │
│      • Caching for speed                                    │
│      • Prefetching                                          │
│                                                             │
│   3. Scalability                                            │
│      • Add more servers = more capacity                     │
│                                                             │
│   4. Fault Tolerance                                        │
│      • Replication                                          │
│      • Server failure handling                              │
│                                                             │
│   5. Consistency                                            │
│      • Multiple clients accessing same file                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📂 NFS (Network File System)

```
┌─────────────────────────────────────────────────────────────┐
│                         NFS                                 │
│                                                             │
│   Developed by Sun Microsystems (1984)                     │
│   Most widely used DFS                                      │
│                                                             │
│   Client                              Server                │
│   ┌─────────────────┐               ┌─────────────────┐    │
│   │                 │               │                 │    │
│   │  /mnt/remote ───┼───────────────┼──► /export/share│    │
│   │       │         │   NFS mount   │                 │    │
│   │  Local view     │               │  Server files   │    │
│   │                 │               │                 │    │
│   └─────────────────┘               └─────────────────┘    │
│                                                             │
│   Mount command:                                            │
│   mount -t nfs server:/export/share /mnt/remote            │
│                                                             │
│   After mount, access /mnt/remote as local                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### NFS Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                   NFS Architecture                          │
│                                                             │
│   Client Side                    Server Side                │
│   ┌─────────────────┐           ┌─────────────────┐        │
│   │   Application   │           │                 │        │
│   └────────┬────────┘           │                 │        │
│            │                    │   NFS Server    │        │
│   ┌────────┴────────┐           │                 │        │
│   │    VFS Layer    │           │   (exports      │        │
│   │  (Virtual FS)   │           │    directories) │        │
│   └────────┬────────┘           │                 │        │
│            │                    └────────┬────────┘        │
│   ┌────────┴────────┐                    │                 │
│   │   NFS Client    │                    │                 │
│   │   (nfsd)        │                    │                 │
│   └────────┬────────┘                    │                 │
│            │                             │                 │
│   ┌────────┴─────────────────────────────┴────────┐       │
│   │                    RPC/XDR                     │       │
│   │              Network Transport                 │       │
│   └────────────────────────────────────────────────┘       │
│                                                             │
│   Stateless: Server doesn't keep client state             │
│   Each request is complete and independent                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 HDFS (Hadoop Distributed File System)

```
┌─────────────────────────────────────────────────────────────┐
│                         HDFS                                │
│                                                             │
│   Designed for big data processing                          │
│   Write once, read many times                               │
│                                                             │
│   ┌─────────────────────────────────────────────────────┐  │
│   │                   NameNode                          │  │
│   │              (Master, Metadata)                     │  │
│   │   - File to block mapping                           │  │
│   │   - Block to DataNode mapping                       │  │
│   └─────────────────────────────────────────────────────┘  │
│                          │                                  │
│          ┌───────────────┼───────────────┐                 │
│          │               │               │                 │
│          ▼               ▼               ▼                 │
│   ┌───────────┐   ┌───────────┐   ┌───────────┐           │
│   │ DataNode  │   │ DataNode  │   │ DataNode  │           │
│   │           │   │           │   │           │           │
│   │  Block A  │   │  Block A  │   │  Block B  │           │
│   │  Block B  │   │  Block C  │   │  Block C  │           │
│   │  Block C  │   │  Block B  │   │  Block A  │           │
│   └───────────┘   └───────────┘   └───────────┘           │
│                                                             │
│   Features:                                                 │
│   • Large blocks (128MB default)                           │
│   • Replication (3x default)                               │
│   • Rack awareness                                          │
│   • Fault tolerant                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### HDFS Write Operation

```
┌─────────────────────────────────────────────────────────────┐
│                    HDFS Write                               │
│                                                             │
│   1. Client asks NameNode for DataNodes                    │
│   2. NameNode returns list of DataNodes                    │
│   3. Client writes to first DataNode                       │
│   4. DataNodes replicate (pipeline)                         │
│                                                             │
│   Client ──► DataNode1 ──► DataNode2 ──► DataNode3         │
│              (writes)     (replicates) (replicates)         │
│                                                             │
│   ACK travels back:                                         │
│   Client ◄── DataNode1 ◄── DataNode2 ◄── DataNode3         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Caching in DFS

```
┌─────────────────────────────────────────────────────────────┐
│                     Caching Strategies                      │
│                                                             │
│   1. Client-side caching                                    │
│      ┌────────┐                     ┌────────┐             │
│      │ Client │                     │ Server │             │
│      │ ┌────┐ │ ← First read        │        │             │
│      │ │Cache│ │ ← Subsequent reads │  File  │             │
│      │ └────┘ │   (from cache)      │        │             │
│      └────────┘                     └────────┘             │
│                                                             │
│   2. Cache Consistency:                                     │
│                                                             │
│   Write-through:                                            │
│      Write to cache AND server immediately                  │
│      ✓ Always consistent                                   │
│      ✗ Slower writes                                       │
│                                                             │
│   Write-back:                                               │
│      Write to cache, sync to server later                   │
│      ✓ Faster writes                                       │
│      ✗ Data loss if crash                                  │
│                                                             │
│   3. Invalidation:                                          │
│      Server notifies clients when file changes              │
│      Clients invalidate their cache                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 DFS Comparison

| Feature | NFS | HDFS | AFS |
|---------|-----|------|-----|
| Purpose | General | Big Data | WAN |
| Caching | Client | DataNode | Aggressive |
| Replication | Optional | Built-in | Yes |
| Consistency | Weak | Eventual | Strong |
| Stateful | No (v3) | Yes | Yes |

---

## ❓ গুরুত্বপূর্ণ প্রশ্ন

**প্রশ্ন ১:** NFS stateless কেন?
**উত্তর:**
- Server crash থেকে recover সহজ
- Client state মনে রাখতে হয় না
- Server scalable
- কিন্তু প্রতি request এ extra info পাঠাতে হয়

**প্রশ্ন ২:** HDFS কেন large block size ব্যবহার করে?
**উত্তর:**
- Seek time minimize (disk)
- NameNode metadata কম (fewer blocks)
- Sequential read optimized
- Big data processing এর জন্য suitable

**প্রশ্ন ৩:** Cache consistency কেন কঠিন?
**উত্তর:**
- Multiple clients এ same file এর cache
- একজন update করলে সবার cache invalid
- Network latency তে inconsistency
- Trade-off: Consistency vs Performance

---
**পূর্ববর্তী:** [Synchronization](03-synchronization.md)  
**পরবর্তী ফোল্ডার:** [Advanced Topics](../12-Advanced-Topics/README.md)
